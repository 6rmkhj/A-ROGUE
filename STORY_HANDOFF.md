# A:\ROGUE 스토리 개편 인수인계

작성일: 2026-09-08
작업 위치: `C:\Users\admin\OneDrive\Desktop\A-ROGUE`
대상: 다음 시나리오 작성자 및 게임 구현 담당자

## 1. 지금 상태부터 확인

**새 스토리는 원고에만 작성되어 있다. 게임에는 적용하지 않았다.**

최신 원고는 [STORY_REBUILD.md](STORY_REBUILD.md)다. 제목은 「무엇을 남길 것인가」이며, 중반 선택과 다섯 엔딩을 포함한다.

이번 대화에서는 스토리 초안 작성과 수정만 진행했다. C++ 코드, 번역, 세이브 형식, 실행 파일을 이번 스토리에 맞춰 수정하거나 빌드하지 않았다. 게임 테스트를 통과한 구현으로 간주하면 안 된다.

## 2. 사용자의 요청과 우선순위

사용자는 기존 스토리를 전면 재구성하고 싶어 한다. 언더테일·스컬·프로젝트 문 게임들·산나비를 레퍼런스로 제시했다.

첫 개작에 대해 사용자가 직접 지적한 문제:

> 엔딩을 여러 갈래로 두고 플레이어가 선택하게 하고 싶다. 지금은 너무 하나의 선택지만 강요하는 느낌이다.

이 요구가 최우선이다. 마지막에 선택 버튼만 늘리는 것으로 해결하지 않는다. 중반부터 서로 다른 가치를 지지할 근거와 인물 관계를 만들고, 최종 대면의 목적과 주인공 대사도 선택에 맞춰 바꿔야 한다.

구분할 것:

- **사용자가 명확히 요구한 방향:** 복수 엔딩, 실제 플레이어 선택권, 특정 결말을 강요하지 않는 이야기.
- **현재 원고가 제안한 구체안:** 다섯 엔딩, 십칠 및 조연 설정, 볼륨별 여섯 선택, 병합 조건 등. 사용자가 각 세부 항목을 개별 확정한 것은 아니다.
- **현재 요청의 완료 범위:** 인수인계 문서 작성. 이 문서를 만드는 김에 게임 구현까지 자동으로 시작하지 않는다.

## 3. 파일의 우선순위

| 파일 | 용도와 주의점 |
|---|---|
| `STORY_REBUILD.md` | 최신 개작 원고. 향후 작업의 기준. 1~5절은 세계·인물·여섯 볼륨, 6~10절은 선택·최종 볼륨·다섯 엔딩·구현 조건이다. |
| `STORY_REWRITE.md` | 이전 별도 초안. 선임 사본의 무덤, 빈 호스트, 자기 삭제 중심의 내용이다. 최신 원고와 섞지 않는다. |
| `STORY_PLAN.md` | 기존 스토리 개선 계획. 현재 코드의 배경을 이해할 때 참고한다. |
| `CAMPAIGN_PLAN.md` | 여섯 일반 볼륨과 최종 볼륨의 캠페인 설계 배경. |
| `README.md` | 구현 구조·조작·빌드·검사 설명. 과거 설명과 최신 설명이 함께 있어 실제 코드를 대조한다. |
| `RULEBOOK.md` | 전투와 진행 규칙 확인용. |
| `STORY_HANDOFF.md` | 이 인수인계 문서. |

예전 개작에서 제시한 ‘호스트가 처음부터 비어 있었다’, ‘영구적으로 덱을 잃는다’, ‘자기 삭제만 반복을 끝낸다’는 설정을 최신안에 되살리지 않는다.

## 4. 최신 스토리의 골격

고장 난 컴퓨터에서 복구 실행체 A가 깨어난다. 통신 담당 선임 십칠의 안내로 여섯 볼륨을 복구한다. 십칠은 과거 구조 과정에서 다른 프로그램의 기억을 동의 없이 지웠고, 그 책임을 갚으려 자신을 구조 명단에서 제외했다.

이 사실은 공통 진실이다. **십칠을 구하는 것은 공통 목표가 아니다.**

이야기의 질문은 ‘모두를 남길 수 없을 때, 무엇을 남기고 그 결정권을 누구에게 줄 것인가’다. 원래 사용자들의 기록, 현재 실행체의 생존, 피해자의 결정권, A의 독립이 충돌한다.

### 인물의 역할

- **A:** 플레이어. 구조·용서·희생·복종·독립을 자동 대사로 결정하지 않는다.
- **십칠:** 실제로 도움을 주는 선임이자 과거 피해의 책임자. 구출 여부, 전송 동의, 관리자 권한은 서로 다른 문제다.
- **목록:** D:에서 만나는 피해자. 자기 기억과 결정권을 먼저 지킨다. 십칠을 용서하거나 구조하라고 대신 설교하지 않는다.
- **경비:** C:에서 원본과 부재한 주인들의 권리를 주장한다. 호스트 복원에도 감정적 근거를 부여한다.
- **나루:** E:의 전송 안내 서비스. 공동 이주뿐 아니라 A의 독립도 정당한 선택으로 다룬다.
- **중계 서비스:** N:에서 분산 통신 및 저전력 운영의 가능성을 보여 준다. 정착 결말의 사전 근거다.
- **YUN:** 자리에 없는 실제 사용자. 마지막에 등장해 어떤 선택이 정답인지 인증하지 않는다.

### 볼륨별 핵심

| 볼륨 | 사건 | 추가할 필수 선택 |
|---|---|---|
| C: SYSTEM | 보안 기능을 손상시킨 구조의 대가, 원본의 가치 | 경비 권한 유지 / 구조자에게 분산 |
| D: ARCHIVE | 목록의 기억 삭제와 동의 없는 결정 | 증거 전체 공개 / 피해 당사자에게 우선 전달 |
| E: REMOVABLE | 실재하는 출구, 십칠의 빈 예약, 제한된 용량 | 공동 이주 / 독립 출구 / 호스트 복원 준비 |
| N: NETWORK | 실제 대화와 사전 녹음, 통신 의존 | 중앙 통신 / 중계 권한 분산 |
| R: RAMDISK | 개인 기억과 기능의 차이 | 독립 실행 유지 / 공통 코드 공유 시험 |
| X: QUARANTINE | 실제 장애와 십칠의 별도 책임 | 십칠 작업 권한 유지 / 회수 후 심사 |

누적 3·5클리어 시점에도 입장을 묻는다. 예전 답변을 바꾸는 것은 허용한다. 대사·관계는 달라져도 기본 엔딩을 몰래 잠그지 않는다.

## 5. 최종 볼륨의 목적과 보스

A:는 십칠 구출을 위한 필수 여정이 아니라 **자동 명령을 멈추고 플레이어의 결정을 실행할 제어권을 확보하는 장소**다.

| 보스 | 최신 역할 |
|---|---|
| SIGNATURE | 과거 명령과 현재 계획 사이의 잠금을 해제한다. 승인 문구는 플레이어의 방향에 따라 달라진다. |
| SEVENTEENTH | 십칠 혼자 만든 계획의 수정 권한을 협상하거나 확보한다. 협상 경로에서는 함께 자동 방어 루틴을 정지시킨다. |
| LAST.WRITE | 수동 계획을 덮어쓰는 자동 정리를 중단한다. 십칠·경비·통신망 중 조건에 맞는 안내자가 등장한다. |

‘데려가서 물어보려고요’ 같은 구조 결심 대사는 구출을 주장한 경로에만 사용한다. 복원·독립 경로에도 고유 대사를 준다.

## 6. 다섯 엔딩 요약

아래 식별자는 시나리오상의 제안이다. 현재 코드에 다섯 개가 존재하는 것은 아니다.

| 제안 식별자 | 제목 | 선택하는 가치 | 손실·책임 |
|---|---|---|---|
| RESTORE HOST | 돌려놓은 것들 | 부재한 사용자들의 원본을 복원 | 현재 실행체들의 인격 상태는 그대로 이어지지 않음 |
| EVACUATE | 다음 주소 | 현재 실행체의 연속성을 지키며 이주 | 완전한 호스트 이미지와 일부 과거 기록 포기 |
| KEEP RUNNING | 여기서 산다 | 남아서 함께 결정하고 운영 | 계속되는 수리·배분 노동과 장치 손상 위험 |
| EXEC SELF | 나의 이름으로 | A의 독립과 자기 생존 | 다른 이들의 미래를 끝까지 책임지지 않음 |
| MERGE SELF | 우리가 된 다음 | 동의한 병합으로 더 많은 기록 계승 | 독립된 현재 인격의 연속성을 보장할 수 없음 |

세부 규칙:

- 기본 네 경로는 중간 답변과 관계없이 마지막에 검토할 수 있다.
- 병합만 코드 검증과 당사자 동의가 필요하다. 중간 시험을 놓쳤어도 최종 확정 전에 시험 가능하다.
- 십칠을 동행시키지 않아도 공동 이주할 수 있다. 동행 여부와 관리자 권한을 따로 결정한다.
- 독립 엔딩은 사용하지 않는 외부 공간을 독점하지 않는다. 인수인계 여부에 따라 관계와 소식이 달라진다.
- 정착은 무한 생존이나 완전 수리가 아니다. 손상을 늦추며 운영을 계속한다.
- 병합은 모든 것을 구하는 상위 해답이 아니다. 호스트 전체를 보존하지 못하며 정체성의 대가가 있다.
- 각 결말에 고유 마지막 장면과 동등한 완결 분량을 준다. 진엔딩·배드엔딩 표식을 붙이지 않는다.

## 7. 현재 코드에서 확인한 사실

2026-09-08 확인 기준:

- `src/data.h`: `ENDING_COUNT`는 3. `StoryKind`와 `STORY_ENDING_DATA`도 기존 세 엔딩을 전제로 한다.
- `src/campaign.h`: `CampaignState`는 `cleared[6]`, `finalCleared`, `endingSeen[3]`, 메타데이터와 체크섬을 가진다. 중간 결정이나 실패 횟수는 없다.
- `src/campaign.cpp`: `CAMPAIGN_VERSION`은 1. 바이트 단위 직렬화를 하므로 구조체 배열 크기만 바꿔서는 안 된다.
- `src/game.cpp`: 최종 보스 기록 → 진실 → 엔딩 선택 흐름이다. 엔딩별 스토리 매핑도 세 갈래다.
- 일반 볼륨 보스 기록과 선택 LOGS, 조각 회수 구조는 존재한다. 새 관계 분기나 다섯 결말의 구현은 없다.

주요 수정 지점:

| 파일 | 예상 작업 |
|---|---|
| `src/data.h` | 원고 분할, 스토리 종류·선택 데이터·엔딩 수와 매핑 |
| `src/game.h`, `src/game.cpp` | 결정 상태, 조건부 장면, 협상별 연출, 최종 계획 검토와 실행 |
| `src/campaign.h`, `src/campaign.cpp` | 선택 저장, 엔딩 기록 확장, 저장 버전과 구버전 변환 |
| `src/screens.cpp`, `src/main.cpp` | 선택 화면·입력·결과 미리보기·다섯 엔딩 배치 |
| `translations.tsv` | 새 대사와 UI 번역. 원본 수정 후 배포용 복사 절차 확인 |
| `src/smoke.cpp` 및 기존 검사 도구 | 진행·분기·저장·입력·회귀 검증 |

## 8. 다음 담당자의 권장 작업 순서

1. `STORY_REBUILD.md`를 읽고 모든 인물·엔딩·공간 조건을 같은 규칙으로 정리한다.
2. 결정표를 만든다. 각 선택의 시점, 저장 값, 즉시 반응, 후반 영향, 변경 가능 시점을 명시한다.
3. 호스트·실행체·기록의 보존량 표를 확정한다. 어떤 결말에서는 갑자기 모두 들어가는 모순이 없어야 한다.
4. 원고를 화면당 3~5줄로 분할하고 모든 필수 선택지·응답·거절·후일담을 작성한다. 현재 원고는 장면 설계이며 모든 조건 조합의 최종 문자열이 완성된 데이터는 아니다.
5. 구현 요청이 이어지면 상태와 저장 마이그레이션을 먼저 작업한다. 완료한 볼륨과 그 결정을 함께 저장해 재시도 시 중복 적용을 막는다.
6. 선택 장면과 최종 검토 화면을 연결한 뒤, 엔딩과 조건부 후일담을 연결한다.
7. 번역·시각 검토·진행 테스트·기존 회귀 검사를 실행한다.

새 게임 규칙이나 전투 밸런스를 바꿔야만 스토리가 성립하도록 범위를 불필요하게 늘리지 않는다. 기존 보스 기믹을 활용하되 대사만으로 미구현 효과를 주장하지 않는다.

## 9. 미결정 사항과 구현 전 해소할 점

- 실제 저장 공간 수치 및 엔딩별 보존량은 미정이다. 전투 작업 용량 240B·180B·130B를 인격 파일 전체 크기로 사용하지 않는다.
- 엔딩별 선택·처우의 조합 수와 표시 방식은 정해야 한다. 모든 조합을 별도 엔딩으로 늘리기보다 공통 본문과 조건부 후일담으로 관리한다.
- 복원 반대자의 거부와 플레이어의 최종 결정권이 충돌할 때의 대사를 구체화해야 한다. 전원이 자동 동의하는 연출은 피한다.
- 십칠이 전송에 동의하는 정확한 조건, 협상 과정과 병합 거절 응답은 작성이 더 필요하다.
- 저전력 정착의 유지 범위와 외부 백업 한도를 명시해야 한다. 다른 결말보다 손실 없이 모두 구하는 방법처럼 만들지 않는다.
- 이름 입력은 실제 입력·저장 기능 또는 마지막 화면의 빈칸 연출 중 선택할 수 있다.
- 구버전 세 엔딩 기록은 새 분기 선택의 증거가 아니다. 과거 기록으로 보존하고 중간 선택을 임의 추정하지 않는다.
- 엔딩 재열람용 최종 확정 전 상태를 어떻게 저장할지 정해야 한다. 중간 관계를 바꾸는 새 캠페인과 구분한다.

## 10. 완료 판정과 검증

스토리 검토:

- 십칠을 구하지 않는 플레이에서도 주인공의 대사·목표가 일치한다.
- 원본 복원, 이주, 정착, 독립 모두 중반부터 감정적 근거가 있다.
- 플레이어가 입장을 바꿀 수 있고 그 대가가 명확하다.
- 모든 경로에서 핵심 진실을 알 수 있다. 선택 LOGS 누락이 필수 사실을 숨기지 않는다.
- 어떤 엔딩도 다른 엔딩을 보라는 광고처럼 끝나지 않는다.

구현 검토:

- 볼륨 방문 순서와 무관하게 아직 만나지 않은 인물이나 사건을 대사가 전제하지 않는다.
- 기본 네 경로 접근, 병합 검증·동의·거절·철회가 정상 동작한다.
- 보존 대상·손실·A와 십칠 상태·불확실성을 최종 확정 전에 확인할 수 있다.
- 저장·재로드·사망 후 재시도에서 완료한 결정이 보존되고 중복 적용되지 않는다.
- 구버전·손상·지원하지 않는 세이브 처리를 기존 정책과 함께 검증한다.
- 키보드와 마우스로 모든 선택에 접근 가능하며 한·영 및 배율별로 글자가 잘리지 않는다.
- 변경 범위에 맞춰 빌드·스모크·기존 화면 검사와 패키지 용량 한도를 확인한다. 과거 README의 통과 결과를 이번 작업 결과로 재사용하지 않는다.

## 11. 작업 파일과 주의 사항

인수인계 작성 직전 `git status --short`에서 확인:

- 미추적: `STORY_REBUILD.md`, `STORY_REWRITE.md`.
- 기존 변경: `build/AROGUE.exe`, `build/balance.exe`, `build/curve.exe`, `build/smoke.exe`, `build/wav.exe`.

위 빌드 파일 변경은 이번 스토리 작성으로 만든 결과가 아니다. 임의로 되돌리거나 새 시나리오 적용 실행 파일이라고 소개하지 않는다. 커밋·배포는 수행하지 않았다.

환경 메모: 일반 파일 읽기에서 OneDrive 경로 접근 거부가 발생한 적이 있다. 이후 정확한 프로젝트 경로만 대상으로 권한 검토를 거쳐 읽고 썼다. 명령을 실행할 때 작업 경로를 명시하고, 경로 이동 실패 시 즉시 중단하여 다른 디렉터리를 검색하지 않도록 한다.

---

다음 작업을 시작할 때의 한 문장:

**최신 원고의 다섯 결말을 기준으로, 플레이어가 십칠 구출을 선택하지 않아도 처음부터 끝까지 자기 판단으로 이야기를 완결할 수 있게 만든다.**
