# A:\ROGUE 드라이브별 보스 기믹 명세서 v2.0

기존 공용 보스 3종(디스크 오류·부트 섹터·포맷) 설계서를 드라이브별 18보스 명세로 확장한 문서다. 레거시 보스 3종은 `GIMMICK_NONE`으로 enum에 보존되며 활성 로스터에서는 참조되지 않는다.

## 설계 원칙 (v1에서 계승)

기믹은 수치를 키우는 것이 아니라 플레이어 시스템의 다른 층을 공격한다.

1. **배치** — 어느 주사위를 어느 슬롯에 놓을지 (C:\ 잠금, E:\ 오프라인)
2. **해결 순서** — `증폭 → 공격 → 방어 → 연쇄` 규칙 (N:\ 역전)
3. **템포** — 피해를 넣는 속도 (D:\ 복원, R:\ 압력, X:\ 오염)
4. **덱** — 18개 면이라는 자산 (X:\ 격리·영구 삭제)

공통 규칙:

- 모든 발동은 최소 한 턴 전에 예고되며 적 카드·슬롯·주사위 UI에 직접 표시된다.
- 임시 상태(잠금·오프라인·역전·격리)는 승리·패배·재시작 어느 경로로 전투가 끝나도 정리된다. 유일한 영구 효과는 ZERO.DAY의 면 삭제다.
- 보스가 효과 발동 전에 죽으면 사후 효과는 실행되지 않는다.
- 피해 임계는 방어도를 뚫고 들어간 실제 체력 피해만 센다.
- 6개 GimmickFamily(테마)와 21개 BossGimmickKind(고유 행동)를 구분한다. 같은 계열은 공용 런타임(잠금 배열, 게이지, 오프라인 플래그, 체크포인트)을 재사용하지만 발동·예고·효과·대응 조합은 서로 다르다.

## 공용 런타임과 훅

| 훅 | 위치 | 책임 |
|---|---|---|
| 전투 시작 | `GimmickInitCombat` | 게이지·카운트다운·체크포인트 초기화 |
| 턴 시작 | `GimmickTurnBegin` | 이번 턴 잠금·오프라인·역전 확정, 다음 턴 예고 |
| 배치 요청 | `AssignDieToSlot` | 잠긴 슬롯 거부 (실패 반환 → 성공 효과음 억제) |
| 플레이어 해결 | `ResolvePlayer` + `ResolveContext` | 슬롯별 해결기, 역전 순서 지원 |
| 피해 적용 | `DamageEnemy` | 보스 피해를 턴·창 단위로 집계 |
| 적 행동 | `PlanBoss` / `ResolveEnemies` | 강화 공격·대기 의도 반영 |
| 턴 종료 | `GimmickTurnEnd` | 게이지·복원·격리 타이머·카운트다운 진행 |
| 전투 종료 | `GimmickCombatEnd` | 모든 임시 상태 정리 (조기 return보다 먼저 실행) |

## 18행 보스 명세표

수치 표기: HP/공격/방어. 새 보스는 층별 개체가 달라 층 성장 없이 최종 수치를 직접 가진다. 과잉 할당(+30% HP)과 N:\ 특성(-10% HP)은 그대로 적용된다.

### C:\ SYSTEM — FAM_LOCK (슬롯 권한 잠금)

| 항목 | 1층 ACCESS.DENIED | 2층 KERNEL.PANIC | 3층 BLUE.SCREEN — **파쇄** |
|---|---|---|---|
| 기믹 | GIMMICK_ACCESS_DENIED | GIMMICK_KERNEL_PANIC | GIMMICK_BLUE_SCREEN (enum 유지, 동작 교체) |
| 발동 | 짝수 턴 | 2턴부터 매 턴 | 3의 배수 턴, **실행을 누르는 순간** (플레이어 해결보다 먼저) |
| 대상 | 증폭→공격→방어→연쇄 순환 1개 | 직전 턴 출력이 가장 컸던 슬롯 | 방어→증폭→연쇄 순환 1개. 공격은 치지 않는다 (밸런스 검사로 재검토) |
| 예고 | 직전 턴에 대상 슬롯 표시 | 규칙 자체가 예고 (내 배치가 다음 잠금을 결정) | 직전 턴에 대상 칸 표시. 파쇄 중에는 다음 예고 없음 |
| 효과 | 잠긴 슬롯 배치 거부 + 해결 출력 0 | 동일 | 칸 파괴: 배치 거부 + 출력 0. 이미 놓인 주사위는 미배치로 돌아가 그 턴 쓰이지 않는다 (잃지 않음) |
| 해제 | 턴 종료 시 | 턴 종료 시 | 파쇄 턴 포함 2턴 뒤 턴 시작에 복구 (`[17] 복구 프로그램 가동.`) |
| 회피 | 없음 | 없음 | 예고 턴에 보스 체력 피해 12+ → 빗나감 (LAST.WRITE와 같은 기준) |
| 대응 | 예고를 보고 남은 3슬롯 배치 계획 | 주력 슬롯을 매턴 순환 | 예고 턴에 12+로 끊거나, 예고 칸을 비우고 두 턴을 세 칸으로 버틸 배치를 준비 |
| 수치 | 34/5/4 | 48/7/6 | 62/8/7 (변경 없음) |
| 회귀 | 턴1 무잠금·예고 일치·배치 거부·해제 | 최고 출력 슬롯 잠금 | 예고=발동 일치·12+ 빗나감·놓인 주사위 복귀(출력 0, 주사위 유지)·2턴 유지 후 복구·파쇄 중 예고 없음·공격 불가침·전투 종료 정리 |

> **BLUE.SCREEN 교체 — 시스템 정지 → 파쇄.** 옛 규칙은 3턴마다 증폭+연쇄를 동시에 1턴 잠갔다.
> 잃는 양은 3턴에 칸-턴 2로 같고, 두 칸을 잠깐 잃던 것을 **한 칸을 오래 잃는** 쪽으로 바꿨다.
> 계열이 1층(칸 1개 1턴) → 2층(주력 칸) → 3층(칸 1개 2턴 파괴 후 복구)으로 커진다.
> 연출 시안은 `ui-recovery` 페이지의 파쇄 절.
>
> 구현: `BossRuntime.shredLeft[SLOT_COUNT]`가 칸별 남은 턴을 든다. `GimmickTurnBegin`이 먼저 줄여 0이 된 칸을
> 복구하고(그래야 같은 턴에 다음 파쇄를 예고할 수 있다) 남은 칸을 `lockedSlot`에 올린다. 발동만은 턴 시작이 아니라
> `GimmickShredExecute`가 `ResolvePlayer` 앞에서 처리한다 — 대상 칸의 주사위를 미배치로 돌리고 칸을 잠근다.
> 미리보기는 `PreviewTurn`이 사본에 `EndTurn`을 돌리므로 같은 경로를 탄다. 12+ 판정은 예고 턴의 `GimmickTurnEnd`에서
> LAST.WRITE와 같은 `damageThisTurn`으로 본다. 화면은 `SlotShredTurnsLeft`(빈 구멍)와 `SlotShredPending`(조준)을 읽고,
> 연출은 `DrawShredStrike`(fxB = 되돌아간 주사위 번호, 2800ms)와 `DrawShredRestore`(fxB = `SHRED_FX_RESTORE`, 1900ms)다.
> 표식: `SLOT SHREDDED`.
>
> 밸런스: 교체 후 `balance.exe`의 C:\ 승률은 202/300 → 186/300이다. 휴리스틱은 12+ 회피를 쓰지 않고 예고 칸만
> 비우므로 사람이 낼 수 있는 상한은 이보다 높다. 공격 칸까지 순환에 넣을지는 여전히 밸런스 검사로 정한다.

### D:\ ARCHIVE — FAM_RESTORE (복원·되감기)

| 항목 | 1층 RESTORE.EXE | 2층 TAPE.LOOP | 3층 MASTER.BACKUP |
|---|---|---|---|
| 기믹 | GIMMICK_RESTORE_POINT | GIMMICK_TAPE_LOOP | GIMMICK_MASTER_BACKUP |
| 발동 | 3턴 창 종료 시 누적 피해 < 12 | 매 턴 종료 시 그 턴 피해 < 7 | 체력 40% 미만이 된 턴 종료 시 |
| 예고 | 창 누적/요구 피해 상시 표시 | 이번 턴 피해/요구 상시 표시 | 발동 조건·백업 지점 상시 표시 |
| 효과 | 체크포인트로 복원 (+10 상한) | +5 회복 | 60% 지점으로 복원 (+14 상한) |
| 상한 | 복원 2회, 창마다 체크포인트 갱신 | 총 회복 20 | 1회 |
| 안전 | 현재 체력보다 낮게 되감지 않음, 최대 체력 초과 불가 | 동일 | 동일 |
| 대응 | 창이 닫히기 전 12 피해 | 매턴 7+ 피해 유지 | 임계 직전 버스트로 복원분까지 관통 |
| 수치 | 36/5/4 | 36/7/5 (되감기 포함 실효 ≈56) | 46/8/7 (복원 포함 실효 ≈60) |
| 회귀 | 미달 복원·달성 무복원·2회 상한 | 미달 회복·달성 무회복·총량 상한 | 1회 발동·재발동 없음·상한 준수 |

### E:\ REMOVABLE — FAM_OFFLINE (주사위 연결 끊김)

| 항목 | 1층 AUTOPLAY | 2층 UNSAFE.EJECT | 3층 NO.MEDIA |
|---|---|---|---|
| 기믹 | GIMMICK_AUTOPLAY | GIMMICK_UNSAFE_EJECT | GIMMICK_NO_MEDIA |
| 발동 | 3의 배수 턴 | 짝수 턴 | 매 턴, 단 4의 배수 턴은 인식 휴지 |
| 대상 | 순환하는 주사위 1개 (결정론) | 동일 | 동일 |
| 예고 | 직전 턴에 대상 주사위 표시 | 동일 | 동일 (휴지 턴 포함) |
| 효과 | 해당 턴 그 주사위 출력 0 (`DieState.offline`) | 동일 | 동일 |
| 해제 | 다음 턴 자동 복구 | 동일 | 동일 |
| 상호작용 | 오프라인 발동 턴 조각화 억제, 읽기 오류 재굴림이 먼저, DEFRAG는 오프라인을 막지 않음 | 동일 | 동일 |
| 대응 | 오프라인 예정 주사위에 핵심 역할 금지 | 홀수 턴 화력 집중 | 인식 턴에 최대 화력 |
| 수치 | 35/5/4 | 50/7/5 | 55/8/7 |
| 회귀 | 예고=발동 일치·타 주사위 무영향·복구 | 짝수 발동·홀수 휴지·순환 | 매턴 발동·휴지 턴·조각화 억제·DEFRAG 무관 |

### N:\ NETWORK — FAM_ROUTE (해결 순서 역전)

| 항목 | 1층 PROXY | 2층 ROUTING.LOOP | 3층 TIMEOUT |
|---|---|---|---|
| 기믹 | GIMMICK_PROXY | GIMMICK_ROUTING_LOOP | GIMMICK_TIMEOUT |
| 발동 | 3의 배수 턴 | 짝수 턴 | 카운트다운(3→0)이 0이 되는 턴 |
| 예고 | 직전 턴 역전 예고 | 동일 | 카운트다운 상시 표시 + 직전 턴 예고 |
| 효과 | 그 턴 `연쇄→방어→공격→증폭` | 동일 | 역전 + 보스는 대기(방어), 이후 카운트 리셋 |
| 안전 | 역전 턴 보스 강공·오염 금지 | 동일 | 보스가 아예 공격하지 않음 |
| 대응 | 역전 턴 공격·방어 전념 | 홀수 턴에 증폭·연쇄 | 한 턴 12+ 피해로 카운트 +1 지연 |
| 수치 | 40/5/4 | 56/7/6 | 68/8/7 |
| 회귀 | 증폭 보너스 소실 검증·의도 제한 | 짝수 역전 | 카운트 감소·지연·발동 턴 대기·리셋 |

### R:\ RAMDISK — FAM_PRESSURE (메모리 압력)

| 항목 | 1층 LEAK.DLL | 2층 HEAP.OVERFLOW | 3층 OUT.OF.MEMORY |
|---|---|---|---|
| 기믹 | GIMMICK_LEAK | GIMMICK_HEAP_OVERFLOW | GIMMICK_OUT_OF_MEMORY |
| 게이지 | 최대 4, 턴말 +1 | 최대 3 | 최대 5 |
| 감소 | 한 턴 10+ 피해 → -1 | 12+ → -1 | 14+ → -2 |
| 발동 | 가득 찬 다음 턴 강화 공격, 발동 후 초기화 | 동일 | 동일 |
| 효과 | 강공 +8 | **오염(관통, 방어 절반)** +6 | 강공 +12 |
| 예고 | 게이지 상시 표시 + 가득 시 경고 + 의도 표시 | 동일 | 동일 |
| 취소 | 발동 턴 보스가 먼저 죽으면 불발 | 동일 | 동일 |
| 대응 | 공격을 유지해 게이지 관리 | 방어도 효율이 절반이라 감쇄 우선 | 큰 턴으로 -2 활용 |
| 수치 | 30/5/3 (기믹이 공격을 강제하므로 낮은 HP) | 44/6/5 | 55/8/6 |
| 회귀 | 상승·감소·발동·초기화·선처치 취소 | 관통 검증 | -2 감소 검증 |

### X:\ QUARANTINE — FAM_QUARANTINE (격리·영구 포맷)

| 항목 | 1층 SAMPLE-13 | 2층 SANDBOX.BREACH | 3층 ZERO.DAY (최종) |
|---|---|---|---|
| 기믹 | GIMMICK_SAMPLE13 | GIMMICK_SANDBOX_BREACH | GIMMICK_ZERO_DAY |
| 발동 | 오염 게이지 3 (3턴 주기) | 3의 배수 턴 끝 | 오염 게이지 4 |
| 지연 | 없음 | 없음 | 한 턴 15+ 피해 → 게이지 -1 |
| 대상 | 출력 가능한 면 중 무작위, **발동 1턴 전 예고** | 판의 빈 카드 자리, **1턴 전 예고** | 면 무작위, 1턴 전 예고 |
| 효과 | 전투 동안 격리 (`quarantined`, 출력 0) | **검체 ESCAPEE 1마리 소환** | **영구 EMPTY** |
| 불변 | 격리는 kind·value·damaged·비용 유지 | 소환체는 체력 `p3`%·출력 `BREACH_MINION_POWER`%로 약화 | 삭제는 면 자체가 EMPTY 0B |
| 상한 | 최대 2개 | 동시 1마리 (`p2`) | 출력 가능한 면 1개는 반드시 남김 (불가 시 임시 격리로 대체) |
| 해제 | 전투 종료 시 전부 | 없음. 쓰러뜨려야 사라진다 | 삭제는 전투 후에도 유지 |
| 종료 | 보스 처치 = 전투 종료 | **탈주체까지 전멸해야 종료.** 단 보스가 죽으면 소환은 멈춘다 | 보스 처치 = 전투 종료 |
| 대응 | 예고 면 의존도 축소·격리 전 처치 | 탈주체를 치울지 보스를 끊을지 선택. 연쇄가 탈주체로 튄다 | 15+ 피해로 지연, 남길 면 선택은 운명에 |
| 수치 | 36/5/4 | 50/7/5 | 60/9/8 |
| 회귀 | 예고·불변·상한·종료 해제 | 주기 소환·상한·약화 두 축·보스 사후 정지·전멸 종료 | 영구 유지·최소 잔존 면·게임오버 정리 |

> **소환체가 두 축으로 약해지는 이유.** ESCAPEE는 본체로 두면 피해 7을 내는데,
> 이는 SANDBOX.BREACH 자신(7)과 같다. 체력만 깎으면 사실상 보스가 둘이 된다.
> 실제로 체력 70%·상한 2로는 휴리스틱 승률이 44/300, 상한만 1로 낮춰도 47/300
> 이었고, `EnemyState.power`로 출력까지 45%로 깎고서야 157/300이 됐다.
> 문제는 마릿수가 아니라 소환체의 화력이었다.

## 레거시 보스 (비활성)

| 이름 | enum | 상태 |
|---|---|---|
| 디스크 오류 | `BOSS_DISK_ERROR` | GIMMICK_NONE, 로스터 미참조 |
| 부트 섹터 | `BOSS_BOOT_SECTOR` | GIMMICK_NONE, 로스터 미참조 |
| 포맷 | `BOSS_FORMAT` | GIMMICK_NONE, 로스터 미참조 |

v1의 세 기믹 아이디어(섹터 잠금·부팅 순서 역전·포맷 진행도)는 각각 C:\ 잠금 계열, N:\ 역전 계열, X:\ ZERO.DAY로 확장 이식되었다.

## 검증

- `smoke.cpp`: 로스터·초상화 무결성, 드라이브별 스폰 매트릭스, 위 18개 기믹의 발동·예고·해제·상한·누수 방지 시나리오, 게임오버 경로 정리.
- `balance.cpp`: 드라이브 강제 선택 × 300시드, 기믹 인지 휴리스틱(잠금 회피·역전 턴 공방 전념·오프라인 주사위 회피), 드라이브별 승률·평균 전투 수·보스 도달/처치율 출력. 게이트: 0승 드라이브 또는 1층 보스 도달률 10% 미만이면 실패.


## A:\ROGUE — 최종 볼륨 (캠페인 작업 순서 4단계)

여섯 일반 볼륨을 모두 복구하면 개방된다. 별도 패밀리 없이 기존 연출과 런타임을 재사용한다.

| 보스 | 계열 | 규칙과 대응 |
|---|---|---|
| SIGNATURE | FAM_LOCK | 공격·방어·증폭·연쇄 순으로 검사 슬롯이 이동한다. 해당 슬롯은 법칙 보너스 전 면 출력이 짝수여야 통과한다. 홀수 면은 특수 보너스도 포함해 출력 0. |
| SEVENTEENTH | FAM_RESTORE | 직전 실행에서 가장 높았던 슬롯 산출량(증폭·법칙 포함)을 다음 기본 공격에 더한다. 방어와 연쇄 출력도 복제하며 다음 의도에 합계를 표시한다. |
| LAST.WRITE | FAM_QUARANTINE | 4턴마다 증폭 → 연쇄 → 방어를 영구 봉인한다. 한 턴 보스 체력 피해 12 이상이면 카운트다운이 멈춘다. 공격은 유지하고 새 런에서 봉인을 초기화한다. |

현재 수치는 초기 구현값이다. 최종 볼륨은 balance/curve에서 각각 별도 300개 시드를 검사하며,
본격적인 수치 확정은 캠페인 작업 순서 7단계에서 진행한다.
