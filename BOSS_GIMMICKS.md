# A:\ROGUE 보스 기믹 설계서 v1.0

## 배경

현재 보스 3종은 일반 적과 **완전히 동일한 의도 5종**(공격·강공·방어·복구·오염)을 사용하며, 차이는 수치와 의도 주기뿐이다.

```c
// game.cpp - StartCombat()
if (game->encounter == 2) AddEnemy(game, BOSS_DISK_ERROR + game->floor);
```

```c
// game.cpp - PlanEnemy()
if (enemy->kind >= BOSS_DISK_ERROR) {
    int bossCycle = (game->turn + enemy->kind) % 5;
    ...
}
```

즉 보스는 **체력과 공격력이 큰 일반 적**이다. 층이 내려갈수록 숫자만 커질 뿐 플레이어가 하는 일은 바뀌지 않는다.

### 현재 보스 수치

| 층 | 이름 | enum | 체력 | 과잉 할당 시 | 공격 | 방어 | 5턴 누적 피해 |
|---|---|---|---:|---:|---:|---:|---:|
| 1 | 디스크 오류 | `BOSS_DISK_ERROR` (8) | 38 | 50 | 6 | 5 | 25 |
| 2 | 부트 섹터 | `BOSS_BOOT_SECTOR` (9) | 55 | 72 | 8 | 7 | 36 |
| 3 | 포맷 | `BOSS_FORMAT` (10) | 74 | 97 | 10 | 9 | 47 |

의도 주기는 `(턴 + enum) % 5`이며, enum이 8·9·10으로 달라 세 보스의 시작 지점이 어긋난다.

| | 턴1 | 턴2 | 턴3 | 턴4 | 턴5 |
|---|---|---|---|---|---|
| 디스크 오류 | 강공 9 | 관통 4 | 공격 6 | 방어 5 | 공격 6 |
| 부트 섹터 | 관통 6 | 공격 9 | 방어 9 | 공격 9 | 강공 12 |
| 포맷 | 공격 12 | 방어 13 | 공격 12 | 강공 15 | 관통 8 |

---

## 설계 원칙

기믹은 **수치를 더 키우는 것이 아니라 플레이어 시스템의 다른 층을 공격**한다.

플레이어가 가진 층은 셋이다.

1. **배치** — 어느 주사위를 어느 슬롯에 놓을지
2. **해결 순서** — `증폭 → 공격 → 방어 → 연쇄`라는 규칙
3. **덱** — 18개 면이라는 자산

보스마다 하나씩 맡기면 압박의 **성격**이 층마다 달라지고, 자연스러운 상승 곡선이 만들어진다.

| 보스 | 공격 대상 | 지속 범위 |
|---|---|---|
| 디스크 오류 | 배치 | 그 턴만 |
| 부트 섹터 | 해결 순서 | 그 턴만 |
| 포맷 | 덱 | **영구** |

세 기믹 모두 기존 시스템을 비트는 방식이므로 **새 자원도, 새 화면도 필요 없다.**

---

## 1층 · 디스크 오류 — 섹터 잠금

> 배치를 공격한다

### 규칙

- 매 턴 **슬롯 하나가 잠긴다.** 잠긴 슬롯에는 주사위를 놓을 수 없다.
- 다음 턴에 잠길 슬롯을 **한 턴 전에 미리 표시**한다.
- 잠금은 그 턴이 끝나면 풀린다.

### 왜 통하는가

주사위는 3개, 슬롯은 4개다. 원래도 하나는 비지만 **어느 것을 비울지는 플레이어가 고른다.** 슬롯 하나가 잠기면 남은 3개에 3개를 정확히 배치해야 하므로 **선택의 여지가 사라진다.**

### 대응

잠길 슬롯을 미리 알려주므로 계획이 가능하다.

- 증폭이 잠기는 턴 → 보너스를 포기하고 순수 공격 턴으로
- 공격이 잠기는 턴 → 방어를 쌓고 넘기는 턴으로
- 연쇄가 잠기는 턴 → 손실이 가장 적다

### 왜 1층인가

새 규칙을 배울 필요가 없다. **기존 배치 규칙을 조이기만** 하므로 첫 보스의 학습 부하로 적절하다.

### 구현

- `EnemyState`에 `lockedSlot`, `nextLock` 추가
- `AssignDieToSlot()`에서 잠긴 슬롯 거부
- `SlotPower()`에서 잠긴 슬롯 출력 0 처리
- `DrawSlot()`에 잠금 표시 + 다음 턴 예고 표시

세 기믹 중 **손이 가장 많이 간다** (배치 거부 + UI 두 종류).

---

## 2층 · 부트 섹터 — 부팅 순서 역전

> 해결 순서를 공격한다

### 규칙

- 주기마다 한 턴, **슬롯 해결 순서가 뒤집힌다.**
- `증폭 → 공격 → 방어 → 연쇄` 가 `연쇄 → 방어 → 공격 → 증폭` 이 된다.
- 역전 턴은 의도와 함께 미리 표시한다.

### 왜 통하는가

이 게임에서 **가장 파괴적인 기믹**이다. 수치를 하나도 건드리지 않는데 체감이 가장 크다.

- **증폭이 맨 마지막**에 오면 공격·방어에 아무것도 못 더한다 → 증폭 슬롯 무효
- **연쇄가 맨 처음**에 오면 반복할 직전 효과가 없다 → 연쇄 슬롯 불발

즉 역전 턴에는 **4슬롯 중 2개가 죽는다.**

### 대응

역전 턴에는 증폭·연쇄를 포기하고 **공격·방어에만 주사위를 몰아넣는 것**이 정답이다. 명확한 답이 있으므로 억울하지 않다. 오히려 "이 턴은 단순하게 간다"는 리듬 변화가 생긴다.

### 왜 2층인가

규칙 자체를 건드리므로 플레이어가 슬롯 순서를 이미 이해하고 있어야 한다. 1층 보스를 넘긴 시점이면 충분하다.

### 구현

- `ResolvePlayer()`에 **순서 분기 하나**

슬롯별 계산 로직은 그대로 두고 호출 순서만 바꾸면 된다. 전투 기록(`PushTurnTrace`)도 자동으로 역순으로 찍히므로 별도 작업이 없다.

**세 기믹 중 가장 싸고, 체감은 가장 크다. 여기부터 시작할 것을 권한다.**

---

## 3층 · 포맷 — 포맷 진행도

> 덱을 공격한다

### 규칙

- **포맷 게이지**가 매 턴 차오른다.
- 가득 차면 플레이어의 면 하나가 **영구히 지워진다** (`EMPTY 0B`). 게이지는 초기화되고 다시 찬다.
- 한 턴에 일정량 이상(예: 15) 피해를 넣으면 **게이지가 1 감소**한다.

### 왜 통하는가

최종 보스는 게임 제목 그대로 **디스크를 포맷하려는 존재**다. 그리고 플레이어의 덱이 곧 디스크다. **서사와 메커니즘이 정확히 일치한다.**

지워진 면은 전투가 끝나도 돌아오지 않는다. 장기전으로 끌수록 덱이 실제로 갈려 나간다.

### 대응

게이지를 피해로 늦출 수 있다는 점이 핵심이다. 이 때문에

- "빨리 죽일까 vs 버티며 방어할까"가 아니라
- **"공세를 유지해야 덱이 산다"**

는 압박이 된다. 방어 일변도 플레이를 자연스럽게 막는다.

### 왜 3층인가

**영구 손실**은 런 전체에 영향을 주므로 마지막에 와야 한다. 최종 보스전이므로 이후 전투가 없어 손실이 실제로는 승패에만 작용하는데, 그럼에도 "지워지고 있다"는 압박은 그대로 유효하다.

### 구현

- `EnemyState`에 `formatGauge` 추가
- `DamageRandomFace()`와 거의 동일한 코드 재사용. `damaged = 1` 대신 `kind = FACE_EMPTY`
- 게이지 UI 표시 (적 카드 내부)

---

## 밸런스 주의

`balance.cpp`의 휴리스틱 승률이 현재 **4/300 (1.3%)** 이다. 보스가 더 어려워지면 **0이 될 수 있고**, 그러면 빌드가 실패한다.

```c
// balance.cpp
return wins == 0 ? 1 : 0;
```

기믹을 넣을 때 다음 중 하나가 필요하다.

1. 휴리스틱을 기믹에 대응하도록 개선 (잠긴 슬롯 회피, 역전 턴 인식 등)
2. 게이트 기준을 승률 하한으로 변경
3. 기믹 수치를 승률이 유지되는 선에서 조정

`smoke.cpp`의 9전투 통과 테스트는 적 체력을 1로 강제하므로 세 기믹 모두 그대로 통과한다. **즉 스모크는 이 변경을 검증해 주지 못한다.** 기믹별 회귀 테스트를 별도로 추가하는 편이 좋다.

---

## 구현 순서 제안

| 순서 | 보스 | 이유 |
|---|---|---|
| 1 | **부트 섹터** (순서 역전) | 분기 하나로 끝나는데 체감이 가장 크다. 기믹 방향성을 싸게 검증할 수 있다 |
| 2 | **포맷** (진행도) | 기존 `DamageRandomFace` 재사용. UI는 게이지 하나 |
| 3 | **디스크 오류** (섹터 잠금) | 배치 거부 + 잠금 표시 + 예고 표시로 손이 가장 많이 간다 |

각 기믹은 서로 독립적이므로 하나씩 따로 머지해도 된다.
