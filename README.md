# A:\ROGUE

1.44MB 게임 개발 공모전을 위한 Win32/GDI 로그라이트입니다. 외부 엔진, 이미지, 폰트 파일, 사운드 파일 없이 실행되며 그래픽과 효과음은 Windows API로 생성합니다.

## 구현 범위

- 3개 주사위 × 6면의 18면 덱
- 숫자 및 특수 면 8종
- 공격·방어·증폭·연쇄 슬롯
- 일반 적 8종, 보스 3종
- 3층 × 일반 전투 2회 + 보스전
- 런마다 선택되는 디스크 손상 5종 중 2종
- 보상 면 설치, 층간 용량 정리, 승리·패배·재시작
- GDI 더블 버퍼링 UI와 메모리에서 합성되는 효과음

상세 규칙은 [RULEBOOK.md](RULEBOOK.md)를 참고하세요.

## 빌드

Visual Studio Build Tools와 `Desktop development with C++` 구성요소가 설치된 Windows에서 실행합니다.

```bat
build-msvc.bat
```

스크립트는 다음을 자동으로 수행합니다.

1. `vswhere`로 MSVC를 찾고 x64 환경을 설정
2. `/MT`, `/utf-8`, 크기 최적화로 `build\AROGUE.exe` 생성
3. 모든 손상 조합을 포함한 30개 전체 런 스모크 테스트
4. 300개 시드의 휴리스틱 난이도 표본 검사
5. EXE의 정확한 바이트 수와 1,474,560B 대비 비율 출력

MinGW-w64가 이미 설치된 환경에서는 `build.bat`을 사용할 수 있습니다.

## 조작

| 입력 | 동작 |
|---|---|
| 주사위 클릭 / `1` `2` `3` | 주사위 선택 |
| 슬롯 클릭 | 선택한 주사위 배치 또는 재배치 |
| 적 클릭 | 공격 대상 변경 |
| `Space` | 턴 실행 |
| `Esc` | 선택 주사위 배치 해제 / 보상 건너뛰기 |
| `Enter` | 용량 정리 확정 |
| `R` / `Enter` | 승패 화면에서 새 런 |

## 소스 구조

| 파일 | 역할 |
|---|---|
| `src/data.h` | 면·적·슬롯·손상·층 데이터 |
| `src/game.h` | 게임 상태와 공개 로직 API |
| `src/game.cpp` | RNG, 전투, 보상, 층 진행, 용량 정리 |
| `src/main.cpp` | Win32 창, GDI 렌더링, 입력, 절차적 효과음 |
| `src/smoke.cpp` | 전체 손상 조합과 9전투 상태 전이 검증 |
| `src/balance.cpp` | 고정 휴리스틱으로 전체 런 난이도를 표본 검사하는 개발 도구 |

## 제출 패키지

`build\AROGUE.exe`만 제출 패키지에 포함하면 됩니다. PDB, OBJ, 스모크 테스트 실행 파일과 소스는 실행에 필요하지 않습니다. 공식 한도는 압축 해제 후 전체 1,474,560바이트입니다.

