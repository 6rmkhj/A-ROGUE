# BGM 재작업 계획

지금 음악이 촌스럽게 들리는 이유를 코드에서 찾고, 무엇을 다시 만들지 정한다.

## 진단

`src/music.cpp`는 100줄이고 구조는 옳다 — 정수만 쓰고, 게임 RNG를 건드리지 않고,
샘플 클럭이 정확하다. 문제는 **작곡이 아니라 재생기에 있는 것 세 가지**다.

### 1. 곡이 한 마디짜리다 — 이게 가장 크다

```c
m->stepClock += (uint32_t)(m->bpm * 4);
if (m->stepClock >= MUSIC_RATE * 60u) { ... if (++m->step >= m->stepCount) { m->step = 0; ++m->bar; } }
```

스텝은 16분음표고 `stepCount`는 16이다. 즉 **한 마디를 돌고 처음으로 돌아간다.**

| 드라이브 | BPM | 루프 길이 |
|---|---:|---:|
| C:\ | 100 | **2.40초** |
| D:\ | 88 | 2.73초 |
| E:\ | 108 | 2.22초 |
| N:\ | 124 | 1.94초 |
| R:\ | 142 | **1.69초** |
| X:\ | 99 | 2.27초 |

전투 하나가 8턴, 런 하나가 8전투다. 플레이어는 **같은 2초를 수백 번** 듣는다.
화성도 움직이지 않는다 — 마디마다 같은 근음 위에 같은 음형이 그대로 돌아온다.

`m->bar`는 증가만 하고 **아무도 읽지 않는다.** 마디 개념이 있는데 쓰지 않고 있다.

### 2. 타악기가 드럼이 아니라 잡음이다

```c
if ((p->kickMask >> step) & 1) percussion += 9000;
...
sample += (((int)(m->noiseRng >> 16) - 32768) * percussion) / 32768;
```

`percussion`은 그 스텝 **내내** 일정하다. 킥이 0.15초 동안 같은 세기의 백색소음으로
계속 울린다. 감쇠가 없으므로 "퉁" 이 아니라 "치이이" 다. 킥·스네어·하이햇이 전부
같은 백색소음이고 길이만 다르지도 않다 — 세기만 9000 / 5000 / 1800으로 다르다.

**이것 하나가 "싸구려" 인상의 절반이다.**

### 3. 음에 엔벨로프가 없다

```c
struct MusicChannelState { uint32_t phase; int gain; int targetGain; int note; int envelope; };
```

`gain`, `targetGain`, `note`, `envelope` **네 필드 전부 한 번도 쓰이지 않는다.**
실제로 쓰는 건 `phase` 하나뿐이고, 음은 이렇게 난다.

```c
if (bassOn) sample += Triangle(...) * (mix + 512) / 2304;
```

게이트가 켜지면 최대 음량으로 즉시 시작하고, 꺼지면 즉시 사라진다. 어택도
감쇠도 릴리스도 없다. 파형이 0이 아닌 지점에서 잘리므로 스텝마다 클릭이 난다.
게다가 위상이 음 사이에 초기화되지 않아 음정이 바뀌는 순간에도 튄다.

### 그 밖

- **필터가 없다.** 펄스파를 ±22000으로 그대로 낸다. 효과음 쪽(`audio.cpp`)에는
  이미 1극 저역통과(`lp += (value - lp) * cut / 256`)가 있는데 음악은 안 쓴다.
- **intensity가 편곡이 아니라 볼륨이다.** `mix` 하나를 곱할 뿐이라 위험해져도
  "같은 곡이 커질" 뿐이다.
- **스윙이 없다.** 16분음표가 자로 잰 듯 균일해서 기계적으로 들린다.

---

## 무엇을 만들 것인가

### 부품 1 · 타악기 엔벨로프 — 가장 싸고 가장 크게 바뀐다

스텝 시작으로부터의 경과 샘플을 알아야 하므로 `stepClock`을 그대로 쓴다.

```c
// 스텝 안에서 몇 샘플 지났는가 (0 = 방금 시작)
int intoStep = (int)(m->stepClock / (uint32_t)(m->bpm * 4));
```

| 소리 | 길이 | 모양 |
|---|---|---|
| 킥 | 55ms | 사인, 150Hz → 45Hz 하강 + 지수 감쇠 |
| 스네어 | 130ms | 노이즈 + 200Hz 삼각파, 빠른 감쇠 |
| 하이햇 | 25ms | 노이즈, 고역만 (저역 통과의 반대로 이전 샘플 차분) |

킥을 노이즈가 아니라 **피치가 떨어지는 사인**으로 바꾸는 것이 핵심이다.

> **비용** 1.5시간 · **위험** 낮음 (순수 함수, 결정론 유지)

### 부품 2 · 음 엔벨로프 — 이미 있는 죽은 필드를 쓴다

`MusicChannelState.envelope`를 0~65536 정수로 쓴다. 스텝이 바뀔 때 게이트가
켜져 있으면 어택을 시작하고, 꺼져 있으면 릴리스로 넘긴다.

```c
// 어택 3ms, 감쇠는 채널마다 다르다. 릴리스 20ms로 클릭을 없앤다.
if (gateOn) env += (65536 - env) * ATTACK / 256;
else        env -= env * RELEASE / 256;
```

음이 바뀌는 순간 위상을 0으로 되돌린다 (같은 음이 이어지면 유지). 이것만으로
스텝 경계의 딸깍거림이 사라진다.

> **비용** 1.5시간 · **위험** 낮음

### 부품 3 · 4마디 화성 진행 — "촌스럽다"의 진짜 원인

`m->bar`를 드디어 읽는다. 드라이브마다 4마디 코드 진행을 두고, 그 마디의 근음을
베이스·리드·아르페지오에 **더한다.** 패턴 배열은 그대로 두고 이조만 한다.

```c
// data: 드라이브당 int8_t chord[4] — 반음 단위 근음
static const int8_t CHORD[6][4] = {
    { 0, -3,  -5,  -7 },   // C:\ i - VI - IV - III  차분한 하강
    ...
};
int root = CHORD[drive][m->bar & 3];
```

**한 마디 2.4초가 네 마디 9.6초로 늘고, 화성이 움직인다.** 데이터는 드라이브당
4바이트, 전체 24바이트다.

여기에 A/B 두 벌의 음형을 두고 `(bar >> 2) & 1`로 고르면 **8마디 19초**가 된다.
패턴 표가 두 배(약 720바이트)로 늘지만 그래도 1KB 미만이다.

> **비용** 2시간 · **위험** 낮음 (데이터 추가 + 덧셈 한 번)

### 부품 4 · 저역 통과 필터

`audio.cpp`가 이미 쓰는 1극 필터를 음악에도 건다. 채널마다 `lp` 정수 한 칸.

```c
lp += (value - lp) * cut / 256;   // cut이 낮을수록 어둡다
```

리드·아르페지오의 날카로운 고조파를 깎으면 "삑삑거림"이 "신스"로 들린다.
위독 상태에서 `cut`을 낮추면 소리가 어두워지는 연출도 공짜로 얻는다.

> **비용** 40분 · **위험** 낮음

### 부품 5 · intensity를 편곡으로

지금은 `mix` 곱하기 하나다. 층을 나눈다.

| intensity | 들어오는 것 |
|---|---|
| 0 (디렉터리) | 베이스 + 패드만 |
| 1 (일반전) | + 킥·하이햇 |
| 2 (보스전) | + 리드 |
| 3 (보스 체력 절반 이하) | + 아르페지오·스네어, 필터 개방 |

같은 곡이 커지는 게 아니라 **악기가 들어온다.** 위험도가 소리로 읽힌다.

> **비용** 1시간 · **위험** 낮음

### 부품 6 · 스윙

짝수 16분음표를 약 15% 늦춘다. **스텝 클럭은 건드리지 않는다** — 스모크가
100BPM에서 3308샘플 경계를 정확히 검사하기 때문이다. 대신 게이트가 열리는
시점만 스텝 안에서 미룬다.

> **비용** 40분 · **위험** 중간 (스모크 타이밍 시험과 맞닿아 있다)

---

## 곡 자체

부품 1~6은 재생기다. 그 위에 **여섯 곡을 다시 쓴다.**

| 드라이브 | 성격 | 방향 |
|---|---|---|
| C:\ SYSTEM | 안정·기본 | 느린 4비트, 단조 하강 진행. 가장 얌전하게 |
| D:\ ARCHIVE | 낡은 창고 | 가장 느리게, 리드를 거의 비우고 여백으로 |
| E:\ REMOVABLE | 접촉 불량 | 지금의 고정 dropout을 살려 박자가 빠지는 느낌 |
| N:\ NETWORK | 패킷·지연 | 빠른 아르페지오, 딜레이처럼 반복되는 음형 |
| R:\ RAMDISK | 휘발·과열 | 가장 빠르게, 계속 상승하다 무너지는 진행 |
| X:\ QUARANTINE | 격리·위험 | 15스텝 홀수 박은 유지. 반음 충돌을 의도적으로 |

X:\의 15스텝(엇박)은 좋은 아이디어라 **유지한다.**

---

## 들어볼 방법이 필요하다

**나는 소리를 들을 수 없다.** 파형을 계산해 확인할 수는 있지만 "좋은지"는
판단할 수 없다. 그래서 먼저 만들 것은 곡이 아니라 **감상 도구**다.

```
build/wav.exe  →  music_c.wav, music_d.wav ... 드라이브별 30초
```

`balance.cpp`·`curve.cpp`처럼 개발 전용 콘솔 도구다. `MusicRender`를 그대로
불러 WAV 헤더만 붙여 쓴다. 30줄이면 되고 `AROGUE.exe`에는 들어가지 않는다.

이게 있어야 **바꾸기 전과 후를 직접 비교**할 수 있고, 곡을 고칠 때마다 다시
빌드해 들어볼 수 있다. 없으면 나는 눈으로만 고치게 된다.

> **비용** 40분 · **가장 먼저 만든다**

---

## 순서와 비용

| 단계 | 부품 | 비용 | 왜 이 순서인가 |
|---|---|---:|---|
| 0 | WAV 덤프 도구 | 40분 | 이게 없으면 나머지를 들을 수 없다 |
| 1 | 1 타악기 엔벨로프 | 1.5시간 | 가장 큰 인상 변화 |
| 2 | 2 음 엔벨로프 | 1.5시간 | 클릭 제거. 이후 작업의 바탕 |
| 3 | 3 화성 진행 | 2시간 | "촌스럽다"의 진짜 원인 |
| 4 | 4 필터 | 40분 | |
| 5 | 5 intensity 편곡 | 1시간 | |
| 6 | 곡 여섯 개 재작곡 | 2시간 | 재생기가 좋아진 뒤라야 판단이 선다 |
| 7 | 6 스윙 | 40분 | 선택. 시간이 남으면 |

합계 약 **10시간**. 1·2·3까지만 해도 체감은 대부분 바뀐다.

---

## 지켜야 할 것

`smoke.cpp`의 `CheckDriveRulesStoryAndMusic`이 검사하는 것들이다. 전부 유지한다.

- 같은 입력이면 같은 출력 (결정론)
- `sampleClock`이 렌더한 프레임 수만큼 정확히 증가
- intensity를 바꿔도 시퀀서가 재시작하지 않음
- X:\는 15스텝 마디
- 음소거면 완전한 무음
- 100BPM에서 3308샘플 경계에 정확히 스텝 1
- 긴 렌더에서 클럭이 밀리지 않음

그리고 규약 두 가지:

- **게임 RNG를 절대 건드리지 않는다.** 음악은 자기 `noiseRng`만 쓴다.
- **부동소수점을 쓰지 않는다.** 사인파도 정수 테이블로 만든다.

`BALANCE: 1068/1800`은 음악과 무관하므로 움직이면 안 된다.

---

## 만들지 않을 것

- **외부 음원·MIDI·트래커 파일** — 1.44MB 규칙이자 이 게임의 성격이다.
- **부동소수점 DSP, 리버브, 딜레이 버퍼** — 링 버퍼는 상태를 늘리고 결정론
  검증을 어렵게 한다. 필요하면 아르페지오 음형으로 딜레이를 흉내낸다.
- **스테레오** — 출력이 모노 1채널이다. 바꾸면 `waveOutOpen`부터 손봐야 한다.
- **곡 수 늘리기** — 드라이브 여섯 곡 그대로다. 씬(타이틀·스토리·엔딩)은
  기존처럼 같은 재생기를 다른 설정으로 쓴다.

---

## 결과 (구현 완료)

0~6단계를 전부 구현했다. 7단계(스윙)는 스모크의 100BPM 경계 검사와 맞닿아
있어 빼두었다. 계획과 달라진 곳과 검증 방법만 적는다.

### 만든 것

| 파일 | 내용 |
|---|---|
| `src/music.cpp` | 재생기 전면 재작성. 스텝 트리거 + 성부별 ADSR + 1극 필터 + 타악기 3종 + 층 게인 |
| `src/music.h` | 채널 5개(베이스·리드·아르페지오·패드 2), 타악기 상태, `playGain`·`criticalGain`·`pendingStep` |
| `tools/gen_music.py` | 악보 표기 → `SONG` 표 제자리 생성. **곡은 여기서 고친다** |
| `src/wav.cpp` | 감상·검증 도구. 빌드 스크립트가 `build/wav.exe`로 만들기만 하고 실행은 하지 않는다 |

```
build\wav.exe <폴더>     드라이브별 40초 WAV + 구조 로그
```

### 계획과 달라진 곳

**패턴 표기를 바꿨다.** 게이트 마스크 대신 토큰에 쉼표(`.`)와 붙임(`-`)을 넣어
음·쉼·붙임을 한 자리에서 읽게 했다. 마스크 6개가 사라지고 배열 하나만 남았다.

**위상을 초기화하지 않는다.** 계획은 "음이 바뀌면 위상을 0으로"였지만, 펄스·삼각파는
위상이 이어져도 값이 끊기지 않고 오히려 0으로 되돌리면 그 자리에서 튄다. 어택 램프만
으로 클릭이 사라졌다 (아래 수치).

**드럼 층에 스네어를 넣었다.** 계획은 스네어를 intensity 3에 두었는데 킥·하이햇만으로는
박자가 비어 1로 내렸다. 3에는 아르페지오와 필터 개방만 남는다.

**소환·격리 같은 규칙 계열과 달리 밸런스는 원래 무관하다.** `1068/1800` 그대로다.

### 검증 — 귀 없이 한 것

`wav.exe`가 WAV 뒤에 찍는 구조 로그가 결정적이었다. 시퀀서 상태만 읽으므로 DSP를
거치지 않는다.

```
drive_c   bar 8 [A] bass A2 | bar 1 F | 2 C | 3 G | 4 [B] D | 5 A | 6 F | 7 E
drive_x   Bb B Bb F | Eb B Ab Bb          (반음 충돌 의도)
drive_r   E G A C   | D C B E             (상승 → 붕괴)
```

여섯 곡 모두 8마디 코드 순환과 4마디째 A→B 전환이 로그에 그대로 찍혔고, 리드
트리거가 악보와 일치했다.

| 지표 | 옛 곡 | 새 곡 |
|---|---:|---:|
| 루프 길이 | 1.7~2.7초 (한 마디) | 13.7~21.8초 (8마디) |
| 인접 샘플 급변(클릭)/초 | 476 ~ 2735 | 12 ~ 49 |
| intensity 1→3 RMS 변화 | 2798 → 4516 (볼륨) | 5342 → 6289 (악기 추가) |
| 타이틀 씬 3초 뒤 피크 | — | 0 |
| 클리핑 | 0 | 0 |

스모크 7항목(결정론·샘플 클럭·시퀀서 비재시작·X:\ 15스텝·음소거·100BPM 경계·
장시간 드리프트)은 손대지 않고 전부 통과했다.

### 못 한 것

**소리를 듣지 못했다.** 위 수치는 구조와 신호 품질을 보장하지 자체로 "좋다"를
보장하지 않는다. 감상 도구가 있으니 들어보고 악보를 고치면 된다 — 생성기를 돌리고
빌드하면 끝이다. 스윙(7단계)은 남겨두었다.
