name: Fix new bug issues

on:
  push:
    branches:
      - fix/all-open-issues-20260908

permissions:
  contents: write

jobs:
  patch-and-verify:
    if: ${{ !contains(github.event.head_commit.message, '[new-bugs-applied]') }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: fix/all-open-issues-20260908
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply fixes
        run: python tools/fix_new_bug_issues.py
      - name: Build and run regression suites
        shell: cmd
        run: build-msvc.bat
      - name: Commit verified fixes
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm tools/fix_new_bug_issues.py .github/workflows/fix-new-bug-issues.yml
          git add -A
          git commit -m "fix: resolve save settings platform and docs regressions [new-bugs-applied]"
          git push origin HEAD:fix/all-open-issues-20260908
