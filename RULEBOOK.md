# A:\ROGUE 규칙서 v2.0

## 목표

런 시작 시 탐색할 드라이브(볼륨)를 선택하고, 3개 층의 일반 전투 2회와 보스전을 모두 통과해 최종 보스를 삭제한다. **모든 적과 보스는 선택한 드라이브 전용**이다. 플레이어 HP가 0이 되면 런이 종료된다.

## 드라이브 선택

- 런 시작 시 6종 드라이브 중 3종이 무작위로 제시되며, 하나를 골라 마운트한다.
- 각 드라이브는 카드에 **디스크 손상 2종**이 미리 공개되고 **고유 특성 1개**를 가진다. 드라이브 선택이 곧 이번 런의 손상 조합·적 로스터 선택이다.
- 카드 3장에는 **볼륨 난이도**가 한 등급씩 무작위로 붙는다. 세 장의 등급은 절대 겹치지 않으며, 난이도 역시 카드에 미리 공개된다.
- 보스를 처치하고 다음 층으로 내려갈 때, 선택한 드라이브의 더 깊은 경로로 진입하는 탐색 연출이 재생된다 (예: `C:\ → C:\WINDOWS → C:\WINDOWS\SYSTEM32`). 현재 경로는 헤더에 표시된다.

| 드라이브 | 볼륨명 | 디스크 손상 | 고유 특성 |
|---|---|---|---|
| C:\ | SYSTEM | 배드 섹터 + 체크섬 | 시작 최대 체력 +6 |
| D:\ | ARCHIVE | 배드 섹터 + 과잉 할당 | 모든 층 용량 한도 +15B |
| E:\ | REMOVABLE | 읽기 오류 + 조각화 | 전투 승리 시 체력 5 회복 |
| N:\ | NETWORK | 읽기 오류 + 체크섬 | 모든 적 최대 체력 -10% |
| R:\ | RAMDISK | 조각화 + 체크섬 | 공격 피해 +1, 최대 체력 -4 |
| X:\ | QUARANTINE | 과잉 할당 + 읽기 오류 | 시작 시 무작위 특수 면 1개 설치 |

## 볼륨 난이도

난이도는 **오염(관통) 피해를 받는 비율**이다. 5개 등급 중 3개가 카드에 무작위로, 서로 겹치지 않게 배정된다. 어느 등급이 어느 드라이브에 붙을지는 런마다 달라지므로, 볼륨 특성과 난이도를 저울질하는 것이 첫 선택이 된다.

| 난이도 | 오염(관통) 피해 |
|---|---:|
| 초급자 | 25% |
| 중급자 | 50% |
| 전문가 | 75% |
| 악몽 | 100% (기준값) |
| 광기 | 200% |

- 배율은 **오염(관통) 의도에만** 적용된다. 일반 공격·강화 공격·화상은 난이도의 영향을 받지 않는다.
- 배율은 적 카드에 뜨는 **예고 수치에 이미 반영**되어 있다. `의도: 오염(관통) 8`이면 실제로 들어오는 관통 피해도 8이다.
- 반올림하되 원래 피해가 1 이상이었다면 최소 1은 남는다. 초급자에서도 오염이 완전히 무해해지지는 않는다.
- 마운트한 뒤에는 헤더와 사이드바에 현재 난이도가 계속 표시된다.

## 드라이브별 적과 보스

각 드라이브에는 전용 일반 몹 3종(공격형·방어형·변칙형)과 층별 보스 3종이 있다. 일반 몹 3종은 세 층 모두에 등장하며 `기본 수치 + 성장 × 층`으로 강해진다. 일반전 6회 동안 세 몹이 각각 두 번씩, 마운트 시 시드로 정해진 순서로 등장한다. 보스는 층마다 다른 개체가 나오며 고유 기믹을 가진다.

| 드라이브 | 일반 몹 | 1층 보스 | 2층 보스 | 3층 보스 | 기믹 계열 |
|---|---|---|---|---|---|
| C:\ | DLL.HIJACK · REG.GHOST · WATCHDOG | ACCESS.DENIED | KERNEL.PANIC | BLUE.SCREEN | 슬롯 권한 잠금 |
| D:\ | BIT.ROT · INDEXER · ZIP.BOMB | RESTORE.EXE | TAPE.LOOP | MASTER.BACKUP | 복원·되감기 |
| E:\ | AUTORUN.INF · LOST.CLUSTER · WRITE.PROTECT | AUTOPLAY | UNSAFE.EJECT | NO.MEDIA | 주사위 오프라인 |
| N:\ | SNIFFER · FIREWALL · PING.FLOOD | PROXY | ROUTING.LOOP | TIMEOUT | 해결 순서 역전 |
| R:\ | MEM.LEAK · RACE.COND · DANGLING.PTR | LEAK.DLL | HEAP.OVERFLOW | OUT.OF.MEMORY | 메모리 압력 |
| X:\ | MUTANT.SMP · ESCAPEE · RANSOMWARE | SAMPLE-13 | SANDBOX.BREACH | ZERO.DAY | 격리·영구 포맷 |

## 보스 기믹 규칙

모든 기믹은 **발동 전에 예고**되며 적 카드·슬롯·주사위에 직접 표시된다. 임시 상태(잠금·오프라인·역전·격리)는 전투가 끝나면 반드시 해제된다. 유일한 예외는 `ZERO.DAY`의 영구 삭제다.

- **슬롯 잠금 (C:\)** — 예고된 슬롯에 주사위를 놓을 수 없고 출력도 0이다.
  - ACCESS.DENIED: 짝수 턴마다 슬롯 1개가 잠긴다 (직전 턴에 예고).
  - KERNEL.PANIC: 직전 턴 출력이 가장 컸던 슬롯이 다음 턴 잠긴다. 주력 슬롯을 매턴 바꿔야 한다.
  - BLUE.SCREEN: 3턴마다 증폭·연쇄가 동시에 잠긴다. 정지 턴에는 공격·방어만 남는다.
- **복원 (D:\)** — 요구 피해를 채우지 못하면 보스 체력이 되감긴다. 복원은 예고된 요구치·횟수 상한이 있고, 현재 체력보다 낮게 되감지 않는다.
  - RESTORE.EXE: 3턴 창의 누적 피해가 12 미만이면 체크포인트로 복원 (+10 상한, 최대 2회).
  - TAPE.LOOP: 한 턴 피해가 7 미만이면 턴말 +5 회복 (총 20 상한).
  - MASTER.BACKUP: 체력이 40% 미만이 된 턴말에 1회, 60% 지점으로 복원 (+14 상한).
- **오프라인 (E:\)** — 예고된 주사위 하나가 그 턴 출력 0이 되고 다음 턴 자동 복구된다. 오프라인 발동 턴에는 조각화가 억제되며, 읽기 오류 재굴림이 먼저 처리된다. DEFRAG는 오프라인을 막지 않는다.
  - AUTOPLAY: 3턴마다. UNSAFE.EJECT: 짝수 턴마다. NO.MEDIA: 매턴, 단 4턴마다 인식 턴에는 발동하지 않는다.
- **순서 역전 (N:\)** — 예고된 턴에 해결 순서가 `연쇄 → 방어 → 공격 → 증폭`으로 뒤집힌다. 증폭 보너스는 소실되고 연쇄는 반복할 것이 없어 불발한다. 역전 턴에 보스는 강공·오염을 쓰지 않는다.
  - PROXY: 3턴마다. ROUTING.LOOP: 짝수 턴마다. TIMEOUT: 카운트다운이 0이 되는 턴 역전되지만 보스도 대기(방어)하며, 한 턴 12+ 피해로 카운트를 1 되돌릴 수 있다.
- **메모리 압력 (R:\)** — 게이지가 매턴 오르고 가득 차면 다음 턴 강화 공격이 예고된다. 발동 후 게이지는 초기화된다. 압력 보스들은 기본 체력이 같은 층 보스보다 낮다.
  - LEAK.DLL: 게이지 4, 한 턴 10+ 피해로 -1, 강화 공격 +8.
  - HEAP.OVERFLOW: 게이지 3, 12+ 피해로 -1, 강화 공격이 오염(관통) 판정(+6). 방어도가 절반만 먹히고 볼륨 난이도 배율도 함께 걸린다.
  - OUT.OF.MEMORY: 게이지 5, 14+ 피해로 -2, 강화 공격 +12.
- **격리 (X:\)** — 예고된 면 하나의 출력이 0으로 봉인된다. 격리된 면의 종류·값·비용은 변하지 않으며 전투가 끝나면 해제된다.
  - SAMPLE-13: 오염 게이지가 3이 되면 예고된 면을 전투 동안 격리 (최대 2개).
  - SANDBOX.BREACH: 3턴마다 예고된 면을 2턴 동안 격리.
  - ZERO.DAY (최종 보스): 오염이 4가 되면 예고된 면을 **영구 삭제**한다 (`EMPTY 0B`). 한 턴 15+ 피해로 오염을 1 늦출 수 있다. 출력 가능한 면이 하나만 남으면 그 면은 영구 삭제되지 않고 임시 격리로 대체된다.

## 덱과 용량

- 주사위 3개, 각 6면으로 총 18면이 덱이다.
- 숫자 면은 숫자값과 바이트 비용이 같다. 시작 덱은 `3 × (1+2+3+4+5+6) = 63B`다.
- 특수 면은 고정 비용을 가진다. 손상 면은 효과를 잃지만 비용은 그대로 차지한다.
- 층 한도는 240B, 180B, 130B다. `과잉 할당`이 있으면 현재 층 한도가 60B 증가한다.
- **면과 상주 프로그램(TSR)의 비용 합**이 층 한도와 비교된다. 헤더의 `용량` 표시가 이 합계다.
- 보상 선택 직후 현재 층 한도를 검사하며, 보스전 뒤에는 다음 층 한도도 검사한다. 한도를 넘으면 정리 화면에서 면을 `EMPTY(0B)`로 지우거나 상주 프로그램을 종료해야 진행할 수 있다.

## 상주 프로그램 (TSR)

최종 보스를 제외한 **보스 처치 보상**으로만 나온다 (런당 최대 2회). 후보 3종 중 하나를 클릭하면 즉시 상주하며, 면을 교체하지 않고 순수하게 용량을 더 차지한다. 특정 손상에 대항하는 프로그램(`DEFRAG`, `SCANDISK`)은 이번 런에 그 손상이 있을 때만 후보로 나온다. 정리 화면에서 클릭하면 언인스톨된다.

| 프로그램 | 비용 | 효과 |
|---|---:|---|
| HIMEM.SYS | 20B | 모든 층 용량 한도 +60B |
| DEFRAG | 22B | 조각화로 인한 주사위 비활성을 무효화 |
| SCANDISK | 24B | 층 하강 시 배드 섹터 손상을 무효화 |
| UNDELETE | 26B | 전투 승리 시 체력 6 회복 |
| SMARTDRV | 28B | 전투 첫 턴 방어도 +6 |
| KEYB | 30B | 판독 후 턴마다 한 번, 선택한 주사위를 재굴림 (`K` 키 또는 버튼) |

## 한 턴

1. 주사위 3개를 굴린다.
2. 각 주사위를 서로 다른 슬롯 하나에 배치한다. 슬롯은 4개이므로 하나는 항상 비어 있다.
3. `Space`를 누르면 읽기 오류와 조각화가 적용된 뒤 `증폭 → 공격 → 방어 → 연쇄` 순으로 해결된다.
4. 살아남은 적이 미리 표시한 의도를 실행한다.

적의 `오염(관통)` 의도에는 **방어도가 절반만 적용된다**. 방어도 10으로 관통 12를 맞으면 5만 막아 7이 체력에 들어가고, 막아낸 몫의 두 배(10)가 방어도에서 소모된다. 관통 수치 자체는 볼륨 난이도의 배율을 이미 거친 값이다. 화상은 적 행동 직전에 3 피해를 주며, 마지막 적이 화상으로 삭제되어도 즉시 전투가 끝난다.

`Space` 전에는 자유롭게 재배치할 수 있다. 같은 슬롯에 새 주사위를 놓으면 기존 주사위는 빠진다.

## 실행 전 미리보기

주사위를 배치하면 실행하기 전에 결과가 먼저 보인다. 배치를 바꿀 때마다 즉시 다시 계산된다.

- 각 슬롯 오른쪽 위에 그 슬롯의 **예상 산출량**이 뜬다. `→ 0`이면 그 슬롯은 이번 턴 아무 일도 하지 않는다 (예: 출력 1을 증폭에 넣으면 보너스는 `1 ÷ 2 = 0`).
- 슬롯 줄 위에 **예상 결과**가 뜬다: `적 체력 -N · 내 체력 -M · 획득 방어도 B`. 이 배치로 적이 삭제되면 `적 삭제`가, 내 체력이 0이 되면 `시스템 정지`가 대신 표시된다.
- 미리보기는 **판독을 마친 뒤에만** 나온다. 판독 전에는 굴림 결과가 아직 가려져 있다.
- `읽기 오류` 손상으로 경고된 주사위가 있으면 그 주사위는 `Space`를 누르는 순간 다시 굴러가므로 `읽기 오류로 확정 아님`이 함께 표시된다. 이때의 미리보기는 현재 굴림 기준의 예상이다.
- 역전 턴에는 `역전 턴!`이 함께 표시된다.

## 슬롯

- 공격: 선택한 적에게 면 출력만큼 피해를 준다.
- 방어: 이번 턴 적 공격을 흡수할 방어도를 얻는다. 다음 턴 시작 시 남은 방어도는 사라진다.
- 증폭: 뒤에 실행될 공격과 방어에 출력의 절반을 더한다. 증폭 면은 전체 출력을 더한다.
- 연쇄: 공격이 있었다면 공격을, 없고 방어가 있었다면 방어를 일부 반복한다. 메아리 면은 직전 효과를 전부 반복한다.

## 면 8종

| 면 | 비용 | 규칙 |
|---|---:|---|
| 숫자 | 값과 동일 | 모든 슬롯에서 기본 출력 |
| 화염 | 24B | 공격 +4, 대상에게 2회 화상 |
| 방벽 | 16B | 방어 슬롯 출력 2배 |
| 흡수 | 22B | 공격 피해 일부 회복 |
| 와일드 | 32B | 모든 슬롯에서 높은 출력과 추가 보너스 |
| 증폭 | 18B | 증폭 슬롯에서 보너스 2배 |
| 메아리 | 20B | 연쇄 슬롯에서 직전 효과 100% 반복 |
| 빈 면 | 0B | 효과 없음 |

## 디스크 손상

선택한 드라이브에 공개된 서로 다른 2개가 런 내내 적용된다.

- 배드 섹터: 층 하강 때 무작위 비어 있지 않은 면 하나가 손상된다.
- 읽기 오류: 경고된 주사위가 `Space`를 누르는 순간 다시 굴러간다.
- 조각화: 같은 결과가 여럿이면 뒤쪽 주사위가 그 턴 비활성화된다. 굴림 직후 `FRAGMENTED`로 표시되므로 실행 전에 확인할 수 있다.
- 과잉 할당: 층 한도 +60B, 모든 적 HP +30%.
- 체크섬: 세 굴림 출력의 합이 짝수면 공격 피해 +2.

## 보상과 회복

- 최종 보스를 제외한 전투 승리 후 보상 화면에서 네 가지 중 하나를 고른다. 세 개는 설치할 면이고, 마지막 하나는 `섹터 복구`다.
- 면을 고르면 기존 면 하나와 교체한다. `섹터 복구`(`4`)를 고르면 면을 설치하지 않는 대신 체력을 회복한다. 회복량은 층별로 10, 13, 16이며 체력이 가득 찬 상태에서는 선택할 수 없다.
- `Esc`로 보상을 건너뛸 수 있다.
- 전투 승리 자체로는 체력이 회복되지 않는다. 회복 수단은 `섹터 복구`, 흡수 면, `E:\ REMOVABLE` 드라이브 특성뿐이다.

## 게임 내 가이드

어느 화면에서든 `F1`을 누르거나 헤더의 `가이드 [F1]` 버튼을 클릭한다. 가이드는 두 페이지로 나뉜다: 1페이지는 공통 규칙(턴 흐름, 슬롯, 상태·의도, 손상, 용량, 조작), 2페이지는 현재 마운트한 드라이브의 일반 몹 3종과 층별 보스 3종의 기믹·대응법이다. `←`·`→` 키나 하단 버튼으로 페이지를 이동하고 `F1` 또는 `Esc`로 닫는다.

## 로스터 판독

가이드 2페이지의 로스터 6칸은 처음에 전부 노이즈로 덮여 있다. **그 개체를 처치해야** 해당 칸의 노이즈가 걷히고 정보가 열린다.

- 미판독 칸은 코드명이 헥스·기호로 갈리고(`.`과 `-`의 자리는 남아 형태만 어렴풋이 보인다) 설명 자리는 헥스 덤프로 덮인다. `미판독` 표시가 함께 붙는다.
- 판독하면 일반 몹은 역할·주기·기본 수치가, 보스는 기믹 이름·규칙·대응법이 열린다.
- 페이지 우측 상단에 `판독 N / 6` 진행도가 표시되고, 6칸을 모두 열면 초록으로 바뀐다.
- 판독 상태는 **런 단위**로만 유지된다. 새 런을 시작하면 다시 전부 덮인다.
- 판독 전이라도 불리해지지 않는다. 모든 보스 기믹은 발동 전에 적 카드·슬롯·주사위에 예고되므로, 가이드는 복습용이지 생존에 필요한 정보원이 아니다.
