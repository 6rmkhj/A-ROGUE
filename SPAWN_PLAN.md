# 다수 적 전투 구현 계획

이슈 #32(여러 적이 나오는 시스템)를 SANDBOX.BREACH 소환 기믹으로 구현한다.
곁가지로 이슈 #31(연쇄가 잘 쓰이지 않음)이 함께 풀린다.

## 진단 — 이미 다 만들어져 있고, 켜지지 않았을 뿐이다

조사에서 나온 가장 중요한 사실이다. 다수 적 전투는 **엔진 전체가 이미
`enemyCount`를 순회하도록 쓰여 있다.**

| 부품 | 위치 | 상태 |
|---|---|---|
| 적 배열 3칸 | `game.h` `EnemyState enemies[3]` | 있음 |
| 적 추가 | `game.cpp` `AddEnemy` — 3마리에서 막고 층 성장·볼륨 보정까지 적용 | 있음 |
| 카드 레이아웃 | `screens.cpp:239` `EnemyRect(i) = 28 + i*218` | 3장 자리 잡혀 있음 |
| 카드 그리기 | `screens.cpp:898` `for (i < enemyCount) DrawEnemy(dc, i)` | 순회함 |
| 대상 지정 | `main.cpp` `ClickCombat` → `SelectEnemy` + `SFX_TARGET` | 동작함 |
| 대상 사망 시 이동 | `game.cpp:1399` `FirstLivingEnemy` | 있음 |
| 의도 계획·해결 | `PlanEnemy` / `ResolveEnemies` 전부 순회 | 있음 |
| 보스 판정 | `BossEnemy` / `BossCardIndex` 가 `IsBossKind`로 **찾는다** | 인덱스 무관 |
| 휴리스틱 AI 대상 선택 | `balance.cpp:157` 체력 가장 낮은 적을 고름 | **이미 있음** |

`StartCombat`([game.cpp:1183](src/game.cpp:1183))이 `AddEnemy`를 한 번만 부르는 것이
전부다. 즉 이 작업은 "다수 적 시스템 제작"이 아니라 **소환하는 주체를 만드는 것**이다.

특히 `BossEnemy`가 인덱스가 아니라 `IsBossKind`로 보스를 찾으므로, 졸개를
1·2번 칸에 넣어도 기믹 해결과 연출이 전부 그대로 돈다. 이게 이 계획의 안전
근거다.

---

## 왜 SANDBOX.BREACH인가

X:\ QUARANTINE 2층, 체력 50.

1. **이름이 이미 소환이다.** 격리가 뚫렸다 = 안에 있던 것이 나온다.
2. **지금 기믹이 아래층과 겹친다.** 1층 SAMPLE-13이 "면 격리", 2층이 "면 격리 후
   2턴 뒤 해제"라 사실상 같은 계열이다. 층이 올라가는 느낌이 없다. 소환으로
   바꾸면 X:\가 **격리 → 침입 → 영구삭제**로 확실히 상승한다.
3. **소환할 졸개가 이미 로스터에 있다.** `MOB_X_ESCAPEE`(샌드박스 탈주) —
   이름이 말 그대로 탈주자이고 `PATTERN_ASSAULT`라 순수 압박이다.
4. 예전에 이슈로 올리려던 "격리 해제에 연출이 없다"가 기믹 교체로 함께 사라진다.

계열은 **`FAM_QUARANTINE`을 유지한다.** 새 계열을 만들면 색 매핑·가이드 페이지·
예고 문구 분기를 전부 건드려야 하는데, "격리 구역이 뚫렸다"는 서사가 이미
X:\ 계열 안에서 성립한다. 움직이는 부품을 늘리지 않는다.

---

## 규칙 설계

기존 매개변수 3칸을 그대로 재사용한다 (`data.h`의 `p1, p2, p3`).

| 칸 | 지금 | 바뀐 뒤 |
|---|---|---|
| `p1` | 3 (격리 주기) | **소환 주기 3턴** |
| `p2` | 2 (격리 턴 수) | **동시 졸개 상한 2** |
| `p3` | 2 | **졸개 체력 비율 %** |

```
3턴마다 샌드박스 탈주 1마리가 튀어나온다 (동시 최대 2마리).
한 턴 전에 예고된다.
```

### 예고

새 상태를 추가하지 않는다. 지금 격리 예고가 쓰는 것과 같은 방식으로 턴 번호만
보면 된다.

```c
if ((turn + 1) % gi->p1 == 0 && LivingMinionCount(game) < gi->p2)
    // "예고: 다음 턴 검체 탈주"
```

`BossRuntime`에 필드를 늘리지 않으므로 세이브 구조도 그대로다.

### 전투 종료 조건 — 바꾸지 않는다

`CombatWon`은 `LivingEnemyCount(game) == 0`에서만 호출된다. 즉 **졸개도 다 죽여야
전투가 끝난다.** 이걸 그대로 두는 것이 이 기믹의 핵심 결정이다.

- 졸개를 치우면 보스에게 쓸 화력이 준다
- 보스만 노리면 졸개가 쌓여 맞는 양이 는다

무한히 늘어질 위험은 없다. `GimmickTurnBegin`이 `if (!BossEnemy(game)) return`으로
시작하므로([game.cpp:362](src/game.cpp:362)) **보스가 죽는 순간 소환이 멈춘다.**
남은 졸개 최대 2마리만 정리하면 끝난다.

코드 변경이 0이면서 진짜 선택을 만든다.

---

## 연쇄 개편 (이슈 #31)

연쇄가 안 쓰이는 이유가 코드에 그대로 있다.

```c
int chainTarget = FirstLivingEnemy(game);   // game.cpp:1648
```

연쇄는 **같은 적을 한 번 더** 때린다. 적이 하나뿐이니 연쇄는 공격 보너스와
구분되지 않고, 그래서 공격 슬롯에 좋은 면을 넣는 쪽이 항상 낫다.

적이 여럿이 되면 "다음 적에게 튄다"로 바꿀 수 있다.

```c
// 대상이 아닌 다른 살아 있는 적. 없으면 지금처럼 대상을 한 번 더.
static int ChainTarget(const GameState* game) {
    int primary = FirstLivingEnemy(game);
    for (int i = 0; i < game->enemyCount; ++i)
        if (i != primary && game->enemies[i].alive) return i;
    return primary;
}
```

**이 한 함수가 연쇄에 고유한 역할을 준다.** 다만 이건 X:\뿐 아니라 **모든 전투에
영향을 준다.** 그래서 마지막 단계로 뺐다 — 시간이 부족하면 여기서 자른다.

---

## 손댈 곳

| # | 파일 | 내용 | 비용 |
|---|---|---|---|
| 1 | `data.h` | 기믹 설명·대응·`stamp` 문구, `p1/p2/p3` 의미 교체 | 20분 |
| 2 | `game.cpp` | `GimmickTurnBegin`의 `GIMMICK_SANDBOX_BREACH` 분기를 소환으로. `LivingMinionCount` 추가 | 1시간 |
| 3 | `screens.cpp` | 예고 문구 분기, 졸개 카드가 보스 카드와 구분되게 | 1시간 |
| 4 | `smoke.cpp` | `smoke.cpp:832`의 격리 검증을 소환 검증으로 재작성 | 45분 |
| 5 | `screens.cpp` | `DrawGimmickFx`의 SANDBOX.BREACH 연출을 소환으로 재작성 | 1.5시간 |
| 6 | `game.cpp` | 연쇄 대상 변경 (#31) | 45분 |
| 7 | 문서 | 밸런스 재기준, README·RULEBOOK·BOSS_GIMMICKS 갱신 | 45분 |

합계 약 **6시간**.

### 순서와 안전점

**1 → 2 → 3 → 4에서 한 번 커밋한다.** 여기까지는 규칙이 X:\ 2층 한 보스에만
닿으므로 되돌리기 쉽다. 5(연출)는 표시 전용이라 언제든 붙일 수 있고, 6(연쇄)은
전 전투에 닿으므로 마지막이다.

---

## 검증

### 스모크

`smoke.cpp:832`가 지금은 이렇게 단언한다.

```c
if (s.boss.gimmick != GIMMICK_SANDBOX_BREACH) return Fail("floor2 X boss must use SANDBOX.BREACH");
```

여기에 붙일 것:

- 3턴째에 적이 2마리가 된다
- 6턴째에 3마리가 되고, 그 뒤로는 상한에 걸려 늘지 않는다
- 보스를 죽이면 그 뒤 턴에 소환이 일어나지 않는다
- 졸개만 남은 상태에서 전부 죽이면 `PHASE_REWARD`로 간다
- (6단계 후) 적이 둘일 때 연쇄가 대상이 아닌 쪽을 때린다

### 밸런스 — **여기가 진짜 위험이다**

이번 작업은 **규칙 변경이라 `BALANCE: 952/1800`이 반드시 움직인다.** 지금까지
연출 작업에서 방패로 써 온 "숫자가 그대로면 안전하다"는 기준을 **쓸 수 없다.**

대신 이렇게 본다.

| 지표 | 지금 | 기대 |
|---|---|---|
| 전체 승률 | 952/1800 | 떨어진다 (X:\가 어려워지므로) |
| drive 5 (X:\) 승률 | 176/300 | **여기가 주로 움직여야 한다** |
| drive 0~4 승률 | 169 / 84 / 103 / 248 / 172 | **1~5단계에서는 변동이 없어야 한다** |
| 평균 전투 수 | 8.16 | 거의 그대로 (전투 수가 아니라 길이가 는다) |

**1~5단계를 마친 뒤 drive 0~4가 한 판이라도 움직이면 X:\ 밖으로 샌 것이다.**
이게 이번 작업의 안전망이다. 6단계(연쇄) 이후에는 전 드라이브가 움직이는 게
정상이므로, 그때부터는 이 기준을 쓸 수 없다.

### 눈으로 볼 것

- 카드 3장이 겹치지 않는가 (`EnemyRect` 3장은 x=28~662, 슬롯은 y=408부터)
- 졸개 카드와 보스 카드가 구분되는가 (체력 게이지 폭, 초상화 크기)
- 대상 표시가 어느 카드인지 분명한가
- 소환 순간 새 카드가 어떻게 등장하는가 (5단계)

---

## 만들지 않을 것

- **새 기믹 계열(`FAM_SUMMON`)** — 색 매핑·가이드·예고 분기가 전부 늘어난다.
  X:\ 서사 안에서 `FAM_QUARANTINE`으로 성립한다.
- **졸개 전용 로스터** — `MOB_X_ESCAPEE`를 그대로 쓴다. 새 스프라이트도, 새
  `EnemyKind`도 만들지 않는다.
- **소환 전용 의도(`INTENT_SUMMON`)** — 소환은 `GimmickTurnBegin`에서 일어나고
  적의 의도와 무관하다. `INTENT_COUNT`를 늘리면 의도 표시·스모크·휴리스틱이
  전부 따라 움직인다.
- **다른 보스로의 확대** — 이번엔 SANDBOX.BREACH 하나다. 나머지 17종은 손대지
  않는다.
