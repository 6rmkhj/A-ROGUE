# 몹 기믹 신설 · 보스 기믹 강화 설계 (프로젝트 문 참고판)

커밋 `35a5241` 기준. 현재 기믹은 21종(6계열)이고 전부 보스 전용이다.

---

## 왜 프로젝트 문인가

**Library of Ruina는 주사위 굴림끼리 맞붙는 전투**다. 이 게임과 뼈대가 같다.
로보토미 코퍼레이션의 환상체 설계, 림버스의 상태이상 설계까지 세 작품을 훑으면
"기믹을 잘 만든다"는 평가의 실체가 네 가지 구조로 정리된다.

### 1 · 큐리포트 카운터 — 위협을 숫자로 보여 준다

로보토미의 환상체는 **눈에 보이는 카운터**를 갖는다. 잘못된 작업을 할 때마다
줄어들고, 0이 되면 탈출한다.

핵심은 **플레이어 자신의 선택이 그 숫자를 깎는다**는 것이다. 사고가 나도 "재수가
없었다"가 아니라 "내가 세 번 잘못했다"가 된다.

> **이 게임에 그대로 맞는다.** "모든 것은 예고된다"는 원칙을 깨지 않으면서
> 긴장을 만드는 유일하게 확실한 방법이다. 지금 압력 게이지(R:\)가 이 구조인데,
> **여섯 계열 중 하나에만 있다.**

### 2 · 뺏지 않고 거래한다

프로젝트 문의 기믹은 "~할 수 없다"보다 **"~하면 ~한다"** 쪽이 많다. 작업 종류를
고르고, 어떤 책을 먼저 태울지 고르고, 어느 인격을 먼저 내보낼지 고른다.

> **지금 이 게임의 약점이 정확히 여기다.** 슬롯 잠금·주사위 오프라인·면 격리는
> 전부 **뺏는다.** 플레이어는 선택하는 게 아니라 남은 것으로 버틴다.

### 3 · 맞붙음(clash) — 내 숫자와 적 숫자를 비교한다

Ruina의 전투는 내 주사위와 적 주사위가 직접 겨룬다. 높은 쪽이 이기고 낮은 쪽이
맞는다. 그래서 **적의 숫자를 보고 내 숫자를 고르는** 행위가 매 턴 생긴다.

> 이 게임엔 적도 숫자(의도값)가 있고 나도 숫자(눈)가 있는데 **둘이 만나지 않는다.**
> 내 공격은 그냥 적 체력에서 빠질 뿐이다. 가장 큰 미개척지다.

### 4 · 이해가 곧 무장 (E.G.O.)

환상체를 이해하면 그 환상체에서 나온 장비를 얻는다. 책을 읽으면 그 책의 페이지를
쓴다. **적을 아는 것이 보상으로 직결된다.**

> 이 게임의 로스터 판독은 **정보만** 준다. 힘은 안 준다.

---

## 설계 규칙

### 규칙 1 · 몹은 "성질", 보스는 "사건"

| | 보스 기믹 | 몹 기믹 |
|---|---|---|
| 형태 | 턴마다 일어나는 **사건** | 전투 내내 참인 **성질** |
| 예고 | 필요 (1턴 전) | 불필요 (카드에 항상 적힘) |
| 상태 | `BossRuntime` (판에 하나) | **`EnemyState`에 개체별로** |
| 수명 | 8~15턴 | 2~4턴 |

몹 기믹은 `BossRuntime`을 쓰면 안 된다. 그건 판에 하나뿐이라 `SANDBOX.BREACH`가
적을 둘로 만들면 두 마리가 같은 칸을 밟는다. **개체마다 카운터 한 칸**
(`EnemyState.counter`)을 두는 것은 괜찮고, 오히려 그래야 프로젝트 문식 설계가 된다.

### 규칙 2 · 예고를 절대 깨지 않는다

이 게임의 최대 강점은 누르기 전에 결과가 보인다는 것이다. 의도를 숨기거나 실행
순간 무작위로 정해지는 기믹은 만들지 않는다.

프로젝트 문도 같은 원칙을 지킨다 — 환상체의 작업 확률표도, Ruina의 적 카드도
**미리 볼 수 있다.** 어려움은 정보를 감춰서가 아니라 **선택을 어렵게** 해서 만든다.

---

## 몹 기믹 21종

기믹을 네 유형으로 나눈다. 볼륨마다 유형이 겹치지 않게 배분해 각 볼륨이
**세 가지 다른 사고방식**을 요구하게 했다.

- **[카운터]** 눈에 보이는 숫자. 내 행동이 깎는다. 0이면 사건
- **[거래]** "~하면 ~한다". 뺏지 않고 값을 치르게 한다
- **[맞붙음]** 내 눈과 적 눈을 직접 비교한다
- **[처형]** 죽이는 방법·순서·타이밍이 조건이 된다

### C:\ SYSTEM — 권한과 검증

| 몹 | 유형 | 기믹 | 규칙 |
|---|---|---|---|
| `DLL.HIJACK` | 카운터 | **가로채기 3** | 증폭 슬롯을 쓸 때마다 1 감소. 0이면 이후 **증폭 보너스가 이 적에게** 간다 (방어도로) |
| `REG.GHOST` | 거래 | **레지스트리** | 매 턴 방어도 +2를 스스로 올린다. 단 **공격 눈이 홀수면 그 턴은 올리지 않는다** |
| `WATCHDOG` | 맞붙음 | **감시 필터** | 공격 눈이 **이 적의 의도값보다 크면** 온전히, 작거나 같으면 절반 |

`WATCHDOG`가 Ruina식 맞붙음의 가장 단순한 형태다. 적 카드에 이미 `의도: 방어 4`가
적혀 있으니 **"4보다 큰 눈을 넣어라"** 가 즉시 읽힌다.

### D:\ ARCHIVE — 노후와 색인

| 몹 | 유형 | 기믹 | 규칙 |
|---|---|---|---|
| `BIT.ROT` | 카운터 | **부패 4** | 매 턴 1 감소. 0이 되면 **무작위 면 1개가 이번 전투 동안 손상**. 한 턴 6+ 피해를 주면 1 회복 |
| `INDEXER` | 맞붙음 | **블록 색인** | **4 이상만 온전히**, 3 이하는 절반 |
| `ZIP.BOMB` | 처형 | **압축 해제** | 처치될 때 **6 피해**로 터진다. **한 턴에 방어도 6 이상**을 들고 있으면 막는다 |

`ZIP.BOMB`은 "죽이지 마라"가 아니라 **"죽일 준비를 하고 죽여라"** 다. 뺏는 게 아니라
거래다.

### E:\ REMOVABLE — 접촉 불량

| 몹 | 유형 | 기믹 | 규칙 |
|---|---|---|---|
| `AUTORUN.INF` | 처형 | **자동 실행** | **1턴에 플레이어보다 먼저** 행동한다 |
| `LOST.CLUSTER` | 거래 | **유실** | 처치될 때 무작위 면 1개가 이번 전투 격리. 단 **처치 턴에 연쇄 슬롯을 채웠으면** 격리되지 않는다 |
| `WRITE.PROTECT` | 맞붙음 | **쓰기 방지** | **직전 턴과 같은 눈**으로 공격하면 절반 |

`LOST.CLUSTER`가 연쇄에 처음으로 존재 이유를 준다. 죽은 슬롯을 **살릴 자리로** 쓰는
방식이다.

### N:\ NETWORK — 사본과 지연

| 몹 | 유형 | 기믹 | 규칙 |
|---|---|---|---|
| `SNIFFER` | 카운터 | **도청 3** | 매 턴 1 감소. 0이 되면 **다음 턴 내 최고 눈을 이 적이 복사해 그 값으로 공격**한다. 이 적을 처치하면 초기화 |
| `FIREWALL` | 맞붙음 | **포트 필터** | **홀수 피해만 온전히**, 짝수는 절반 |
| `PING.FLOOD` | 카운터 | **폭주** | 매 턴 자기 피해 +1 누적. **방어 슬롯에 4 이상**을 넣은 턴에는 오르지 않는다 |

`SNIFFER`의 복사는 `SEVENTEENTH` 보스 기믹의 몹 버전이다 — 계열이 이어진다.

### R:\ RAMDISK — 휘발과 경쟁

| 몹 | 유형 | 기믹 | 규칙 |
|---|---|---|---|
| `MEM.LEAK` | 맞붙음 | **누수** | **3 이하 피해가 +3 추가** |
| `RACE.COND` | 거래 | **경쟁 상태** | **의도를 두 개 표시**하고, 플레이어가 **공격 슬롯에 넣은 눈이 짝수면 왼쪽, 홀수면 오른쪽**을 실행 |
| `DANGLING.PTR` | 처형 | **허상 참조** | 처치되어도 **그 턴 행동은 실행**한다 |

`RACE.COND`가 이 목록에서 가장 프로젝트 문답다. 무작위가 아니라 **내가 고르는 것**
이고, 둘 다 보이므로 예고도 지켜진다. 공격 눈이 피해량이자 **선택 스위치**가 된다.

### X:\ QUARANTINE — 격리와 변이

| 몹 | 유형 | 기믹 | 규칙 |
|---|---|---|---|
| `MUTANT.SMP` | 맞붙음 | **변이** | 맞을 때마다 약점이 **홀 ↔ 짝으로 뒤집힌다** (카드에 현재 상태 표시) |
| `ESCAPEE` | 처형 | **탈주** | 체력 30% 이하가 되면 **다음 턴 도망**친다 (보상 없음) |
| `RANSOMWARE` | 거래 | **암호화** | 한 턴 **8 이상** 넣지 못하면 회복. 대신 이 적을 **8+ 한 방으로 처치**하면 보상 후보가 하나 늘어난다 |

`RANSOMWARE`가 **E.G.O.형 보상**의 최소 구현이다. 조건을 지켜 잡으면 보상이 커진다.

### A:\ FINAL — 진실과 사본

| 몹 | 유형 | 기믹 | 규칙 |
|---|---|---|---|
| `FALSE.COPY` | 맞붙음 | **거짓 사본** | **직전 턴과 같은 눈**이면 **2배** 피해를 받는다 |
| `HALF.WRITE` | 카운터 | **미완성 4** | 매 턴 1 감소, 0이면 자기 체력을 절반 회복하고 초기화. **짝수 눈으로 때리면 2 감소** |
| `ECHO.PROC` | 거래 | **반향** | 직전 턴 준 피해의 **절반을 이번 턴 피해로 되돌린다**. 단 그 턴 **방어를 비웠으면** 되돌리지 않는다 |

`WRITE.PROTECT`(같은 눈=벌)와 `FALSE.COPY`(같은 눈=상)가 정반대다. 최종 볼륨이
앞선 볼륨의 습관을 뒤집는 구조다.

---

## 보스 기믹 강화

### 원칙 · "뺏는다"에서 "거래"와 "맞붙음"으로

현재 보스 기믹 대부분은 뺏는다. 슬롯을 잠그고, 주사위를 끊고, 면을 격리한다.
처음엔 놀랍지만 두 번째부터는 "또 하나 못 쓰네"가 된다.

최종 볼륨 세 보스가 이미 다른 길을 보여 준다. `SIGNATURE`는 **짝수 출력만 통과**
시키고, `SEVENTEENTH`는 출력을 **복제**한다. 배치의 계산법 자체를 바꾼다.
프로젝트 문식으로 한 발 더 가면 **여기에 카운터와 면제 조건을 붙이는 것**이다.

### 3층 보스 6종

| 보스 | 현재 | 강화안 | 유형 |
|---|---|---|---|
| `BLUE.SCREEN` | 3턴마다 증폭·연쇄 잠금 | ~~잠긴 슬롯에 주사위를 넣으면 그 주사위가 이번 전투 손상. 대신 잠긴 슬롯을 비워 둔 채 그 턴을 넘기면 다음 잠금이 한 턴 미뤄진다~~ → **파쇄로 교체 확정.** 예고된 칸 1개를 실행 순간 부수고 2턴 뒤 복구. 예고 턴에 12+ 피해면 빗나감 (`BOSS_GIMMICKS.md` C:\ 표) | ~~거래~~ 카운터 |
| `MASTER.BACKUP` | 40% 미만에서 1회 복원 | 복원 시 **받은 피해의 절반을 방어도로 전환.** 복원 전 3턴 안에 **연속으로 10+**를 두 번 넣으면 복원 자체가 무효 | 카운터 |
| `NO.MEDIA` | 매 턴 1개 오프라인 | 오프라인이 **2턴 연속**. 단 오프라인 주사위를 **연쇄 슬롯에 배치**하면 즉시 복구된다 | 거래 |
| `TIMEOUT` | 카운트 0에 역전 + 대기 | 역전이 **3턴 유지**되고 그동안 **증폭 보너스가 보스에게** 간다 | 뺏기→맞붙음 |
| `OUT.OF.MEMORY` | 게이지 가득 → 강화 공격 | 강화 공격 + **최대 체력 -3 (런 동안 영구)**. 게이지가 가득 찬 턴에 **정확히 게이지 수치만큼의 눈**을 공격에 넣으면 무효 | 맞붙음 |
| `ZERO.DAY` | 면 1개 영구 삭제 | 삭제된 면이 **보스에게 넘어가 그 출력으로 공격**한다. 그 면의 출력만큼 한 턴에 피해를 주면 **되찾는다** | 카운터+E.G.O. |

`ZERO.DAY`가 가장 강하다고 본다. 지금은 "면을 잃었다"로 끝나지만, **잃은 면이 나를
때리고, 되찾을 길이 있으면** 삭제 예고가 훨씬 무겁게 읽힌다. 제로데이의 정확한
의미이기도 하다.

### 1·2층 보스는 건드리지 않는다

1·2층 보스는 그 계열을 **가르치는** 역할이다. `ACCESS.DENIED`가 "슬롯이 잠긴다"를
가르쳐야 `BLUE.SCREEN`의 "잘못 넣으면 잃는다"가 성립한다. 로보토미가 ZAYIN 등급
환상체로 규칙을 가르치고 나서 WAW·ALEPH를 내놓는 것과 같다.

2층만 살짝 올릴 여지가 있다.

- `KERNEL.PANIC`: 최고 출력 슬롯 → **슬롯과 그 주사위를 함께** 잠금
- `UNSAFE.EJECT`: 오프라인 1개 → 짝수 턴에 **2개**
- `SANDBOX.BREACH`: 동시 1마리 → 3층에서 **동시 2마리**

---

## 이 설계가 앞선 문제들을 어떻게 건드리나

| 기존 문제 | 어떤 기믹이 건드리나 |
|---|---|
| 연쇄 슬롯이 죽어 있다 (1.6만 턴 배치 0회) | `LOST.CLUSTER`·`NO.MEDIA`가 연쇄를 **면제 조건**으로 쓴다 |
| 낮은 눈이 언제나 쓰레기 | `MEM.LEAK`·`INDEXER`·`OUT.OF.MEMORY`가 특정 값을 정답으로 만든다 |
| 매 턴 결정의 모양이 같다 | 맞붙음 계열이 **적의 숫자를 보고 내 숫자를 고르게** 한다 |
| 로스터 판독이 정보만 준다 | `RANSOMWARE`·`ZERO.DAY`가 **조건 충족 = 보상**으로 잇는다 |

---

## 비용과 위험

| 작업 | 비용 |
|---|---:|
| `EnemyState.counter` + `EnemyInfo.trait` + `TraitInfo` 표 | 2시간 |
| 판정 지점 3곳 (`DamageEnemy` · `PlanMob` · 전투 종료 경로) | 2시간 |
| 몹 기믹 21종 데이터·문구 | 2시간 |
| 몹 카드에 카운터·기믹 배지 표시 | 1.5시간 |
| **미리보기 연동** | 2시간 |
| 보스 3층 6종 강화 | 3시간 |
| 스모크 시나리오 | 2시간 |
| 밸런스 재조율 | 2시간 |

합계 약 **16시간**. 몹 기믹만 먼저 하면 9~10시간이다.

### 반드시 지킬 것 · 미리보기

**맞붙음·필터 계열은 미리보기 숫자에 그대로 반영되어야 한다.** `WATCHDOG`에게
의도값보다 작은 눈을 넣었을 때 미리보기가 온전한 값을 보여 주면, 이 게임에서 가장
좋은 부분이 거짓말이 된다.

`PreviewTurn`이 상태를 복사해 `EndTurn`을 돌리는 구조라 **판정을 `DamageEnemy` 안에
두면 자동으로 따라온다.** 그 위치를 지킬 것.

카운터 계열도 미리보기에 `부패 4 → 3` 처럼 다음 값이 보여야 한다.

### 밸런스

현재:

```
BALANCE: 1051/1800 (58%)
드라이브별: 218 / 172 / 137 / 203 / 144 / 177   (격차 81, 게이트 한계 90)
```

**격차가 81/90으로 이미 빡빡하다.** 그래서 볼륨마다 유형을 **카운터 1 · 거래 1 ·
맞붙음 1 (또는 처형)** 로 균등 배분했다. 한 볼륨만 세지면 게이트가 깨진다.

거래형은 면제 조건이 있어 순수 상향이 아니고, 맞붙음형은 플레이어가 맞추면
오히려 유리하다. 카운터형만 명확한 하향이다. 넣은 뒤 `balance.exe`로 드라이브별
편차를 반드시 다시 볼 것.


---

## 구현 결과

몹 특성 21종과 3층 보스 강화 3종을 구현했다. 계획과 달라진 곳만 적는다.

### 계획이 바뀐 곳

**`AUTORUN.INF` 선공.** 계획은 "1턴에 플레이어보다 먼저 행동"이었다. 그러려면 해결
파이프라인과 연출 재생 순서를 갈라야 해서 위험이 컸다. 대신 **1턴 공격이 방어도를
절반만 인정**하게 했다. 턴 순서를 건드리지 않고 "준비 전에 맞는다"는 체감을 낸다.
처음엔 방어도를 완전히 무시하게 했는데 1턴이 회피 불가가 되어 E:\ 승률이
137 → 112로 떨어졌다.

**보스 강화는 3종만.** `ZERO.DAY` · `MASTER.BACKUP` · `TIMEOUT`을 했다. 나머지 셋
(`BLUE.SCREEN` 잠긴 슬롯 손상, `NO.MEDIA` 2턴 연속, `OUT.OF.MEMORY` 최대 체력 감소)은
넣지 않았다. 세 개만으로 이미 드라이브 격차가 게이트 한계까지 갔고, 더 넣으면
조율할 여유가 없었다.

### 밸런스

```
              C:\   D:\   E:\   N:\   R:\   X:\    총합    격차
이전          218   172   137   203   144   177   1051      81
몹 특성만     165   135   112   202   141   154    909      90
보스 강화 후  165   110   114   192   141   141    863      82   (게이트 한계 90)
```

조율 과정에서 배운 것: **한 볼륨만 세지면 게이트가 바로 깨진다.**
`MASTER.BACKUP`의 방어 전환을 받은 피해의 1/2로 두었더니 D:\가 135 → 89로 떨어져
격차가 113이 됐다. 1/4로 낮추고, `TIMEOUT` 강화로 N:\을 202 → 192로 함께 내려서
82로 닫았다.

### 시험

`CheckMobTraits`가 눈의 값이 실제로 피해를 바꾸는지 못박는다 — 맞붙음 4종, 반복 눈
2종(쓰기 방지 절반 / 거짓 사본 2배), 압축 해제의 폭발과 방어 흡수, 카운터 감소,
그리고 **미리보기가 특성을 실제 해결과 똑같이 반영하는지**.

고정 장치를 만들며 알게 된 것: 눈의 값을 검사하려면 값에 손대는 요소를 전부 빼야
한다. 읽기 오류는 실행 순간 재굴림하고, 체크섬은 합의 홀짝으로 +2를 얹고, C:\ 법칙
(최저 출력 +1)과 R:\ 법칙(공격 +1)은 홀짝까지 뒤집는다. E:\는 "승리 시 체력 8 회복"이
압축 해제 폭발 피해를 그대로 덮었다. 그래서 N:\ + 배드섹터/과잉할당을 쓴다.

### 남은 것

- 보스 강화 2종 (`NO.MEDIA` · `OUT.OF.MEMORY`). `BLUE.SCREEN`은 강화 대신 **파쇄로 교체 확정 · 미구현** (`BOSS_GIMMICKS.md` C:\ 표).
  C:\ 3층 유형이 거래 → 카운터로 바뀌므로, 넣은 뒤 `balance.exe`로 C:\ 편차를 다시 볼 것
- 2층 보스 소폭 상향 3종
- 특성별 발동 연출 — 지금은 로그와 카드 표기로만 알린다
