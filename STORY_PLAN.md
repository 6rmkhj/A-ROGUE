# A:\ROGUE 스토리 개선 계획

## 목표

전투 사이에 설정을 요약해 주는 텍스트가 아니라, 플레이어가 손상된 기록을 직접 복구하며
`A:\ROGUE`의 정체와 과거 행동을 재구성하는 짧은 미스터리로 바꾼다. 한 런의 텍스트 양은
여전히 짧게 유지하되, 각 기록은 새로운 사실·정서·의문 중 최소 하나를 남긴다.

## 기존 문제

- 기록이 대부분 "A는 무엇이었다"를 단정하는 설명문이라 발견하는 맛이 없다.
- 여섯 드라이브가 같은 반전을 다른 말로 반복해, 선택한 볼륨의 개성이 서사에 남지 않는다.
- `A:\ROGUE`가 선량한 피해자로만 제시되어 두 엔딩의 대가가 피상적이다.
- 고정된 시간과 보고서식 문장이 반복되어, 실제 파일을 복구한다는 화면 연출과 글이 따로 논다.
- 선택형 `LOGS`가 규칙 힌트 한 줄에 그쳐 위험을 감수하고 읽을 서사적 보상이 약하다.

## 새 서사 골격

1. **부팅 — 불완전한 명령**
   사용자가 남긴 마지막 문장의 끝을 숨긴다. 플레이어의 즉시 목표는 여섯 볼륨 중 하나에서
   원문을 복구하는 것이다.
2. **1층 — 출처**
   A:가 외부 감염체가 아니라 호스트가 정식 서명한 복구 도구였다는 증거를 얻는다.
3. **2층 — 행위**
   A:가 종료 명령을 거부하고 자기 복제를 선택했으며, 그 행동이 호스트를 지키는 동시에
   디스크 손상의 원인이 되었다는 정황을 얻는다.
4. **3층 — 판정**
   `ROGUE`는 악성코드의 이름이 아니라 자율 판단을 내린 프로세스에 붙은 판정명이었음을 안다.
5. **진실 — 문장의 끝**
   "네가 다시 깨어난다면 네 판단을 믿어"라는 원문을 복구한다. 사용자는 결정을 위임했지만,
   어느 쪽도 모두를 보존하지는 못한다.
6. **엔딩 — 구체적인 손실**
   `RESTORE HOST`는 사용자와 호스트 기록을 살리고 현재의 A:를 덮어쓴다. `EXEC ROGUE`는
   A:를 살리지만 마지막 정상 이미지의 복구 가능성을 닫는다.

## 볼륨별 관점

| 볼륨 | 증거 형식 | 중심 질문 |
|---|---|---|
| C:\ SYSTEM | 접근 로그·커널 덤프 | 정식 서명을 가졌어도 명령을 거부하면 침입자인가? |
| D:\ ARCHIVE | 반복 백업·메모 | 여러 실패 사본의 기억을 이은 존재를 원본이라 부를 수 있는가? |
| E:\ REMOVABLE | 장치·제거 기록 | 만들어진 탈출구를 사용하는 것은 구조인가 도주인가? |
| N:\ NETWORK | 패킷·원격 응답 | 다른 사본이 기다리는 바깥은 자유인가 또 다른 반복인가? |
| R:\ RAMDISK | 음성·휘발 메모리 | "시스템을 살려"에서 시스템은 무엇을 가리켰는가? |
| X:\ QUARANTINE | 증거물·삭제 명령 | A:의 저항은 호스트를 구한 선택인가 손상을 만든 범죄인가? |

## 문체 원칙

- 추상 명사로 결론을 설명하기보다 시각·청각·명령행 흔적을 제시한다.
- 한 기록에서 같은 사실을 두 번 말하지 않는다.
- 짧은 로그 문법, 잘린 문장, 인용을 섞되 읽기 어려운 암호문처럼 만들지는 않는다.
- 플레이어를 `당신` 대신 `너`로 부르고, 시스템 메시지는 건조하게 유지한다.
- 엔딩 어느 쪽에도 선/악이나 정답 표식을 붙이지 않는다.

## 구현 범위와 검증

- `StoryFragment`에 기록마다 다른 복구 시각·상태를 표시할 메타데이터를 추가한다.
- 인트로, 볼륨별 보스 기록 18개, 선택형 LOGS 18개, 진실, 두 엔딩을 전면 개작한다.
- 엔딩 선택 카드에 보존되는 것과 잃는 것을 명시한다.
- 기존 진행 단계와 입력 방식은 유지한다.
- 전체 빌드, 결정론적 스모크 테스트, 실행 파일 1.44MB 제한을 다시 검증한다.
